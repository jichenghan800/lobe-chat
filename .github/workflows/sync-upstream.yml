name: Sync Upstream Logic

permissions:
  contents: write
  pull-requests: write

on:
  schedule:
    - cron: '0 0 * * *' # 每天运行
  workflow_dispatch:

jobs:
  sync_and_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout upstream-sync
        uses: actions/checkout@v4
        with:
          ref: upstream-sync  # <--- 关键：先切到同步分支
          fetch-depth: 0
          token: ${{ secrets.PAT }}

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Sync with Upstream
        id: sync
        run: |
          git remote add upstream https://github.com/lobehub/lobe-chat.git
          git fetch upstream --tags -f
          
          # 获取上游最新 release tag
          # 注意：这里我们使用 rest api 获取准确的 release tag
          LATEST_TAG=$(curl -sSL https://api.github.com/repos/lobehub/lobe-chat/releases/latest | jq -r '.tag_name')
          
          echo "Syncing upstream-sync branch with $LATEST_TAG..."

          # --- 核心操作 ---
          # 强制将 upstream-sync 分支重置为官方的最新 Tag
          # 这样保证 upstream-sync 永远是纯净的官方镜像
          git reset --hard "$LATEST_TAG"
          
          # 强制推送到你仓库的 upstream-sync 分支
          git push origin upstream-sync --force
          
          echo "tag=$LATEST_TAG" >> "$GITHUB_OUTPUT"

      - name: Create Pull Request to Main
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PAT }}
          branch: upstream-sync      #以此分支为源
          base: main                 #合并到 main
          title: 'chore: merge upstream release ${{ steps.sync.outputs.tag }}'
          body: |
            ## 🔄 上游版本同步 ${{ steps.sync.outputs.tag }}
            
            根据三分支模型自动同步：
            `Official` -> `upstream-sync` -> `main`
            
            此 PR 将官方最新代码合并入生产分支。
            如果出现冲突，请在合并前解决。
          labels: 'sync, upstream'
          # 如果已经有一个同样的 PR 开着，通过设置 update-only 防止重复开单
          # 但通常我们需要新的 PR，默认设置即可
