# 仅保留“聊天 + 助理”的导航隐藏模板
# 用途：通过隐藏导航入口来精简界面（功能仍可通过 URL 直接访问）
# 注意：修改 .env 后需要重新 build 才会生效

# —— 顶部/侧边/菜单隐藏开关 ——
# 规则：存在且为 1/true/yes/on 就隐藏；不写则显示

# 顶部/侧边
NEXT_PUBLIC_NAV_HIDE_HOME=0
NEXT_PUBLIC_NAV_HIDE_SEARCH=1
NEXT_PUBLIC_NAV_HIDE_PAGE=1
NEXT_PUBLIC_NAV_HIDE_IMAGE=1
NEXT_PUBLIC_NAV_HIDE_COMMUNITY=1
NEXT_PUBLIC_NAV_HIDE_KNOWLEDGE=1

# 底部（桌面）
NEXT_PUBLIC_NAV_HIDE_SETTINGS=1
NEXT_PUBLIC_NAV_HIDE_RESOURCE=1
NEXT_PUBLIC_NAV_HIDE_MEMORY=1

# 移动端 Tab
NEXT_PUBLIC_NAV_HIDE_CHAT=0
NEXT_PUBLIC_NAV_HIDE_ME=1

# —— 说明 ——
# 1) 保留聊天入口：Home/Chat 均为 0
# 2) 助理入口：默认在“聊天/助手”内，不是独立菜单，因此无需额外开关
# 3) 如果想恢复某个入口，把对应值改为 0 或删除该行
