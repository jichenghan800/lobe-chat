# v2.2.17 最小二开验收环境

## 三步交付

1. 固定 v2.2.16，备份并创建独立数据库副本：已完成。
2. 官方 v2.2.17 加最小二开，部署 chatdev，进行实际验收：进行中。
3. 第二步验收后，一次性合并剩余必要二开，进行完整验收。

2026-09-13 用户调整范围：仅 chatdev.cotticoffee.com 升级；chat.cotti.ai 继续使用原 v2.2.16。生产 chat.cotticoffee.com 不在本次发布范围。

## 基线

- 旧源码：e6e4c774fb，标签 milestone/v2.2.16-before-v2.2.17-20260913，已推送 GitHub。
- 官方源码：v2.2.17 / 787e3d20aa。
- 分支：upgrade/v2.2.17-minimal。
- 私有备份：/opt/backups/lingshu-v2217-baseline-20260913。
- 私有执行记录：.records/v2217-minimal。包含凭据的文件不得提交或对外发布。

## 本阶段实际二开

- 普通飞书、蓝版、Portal OIDC Basic 认证兼容；保留邮箱验证码与既有准入策略。后台准入模型作为登录依赖保留，未合并管理界面。
- 话题金额预算，继承平台 / 用户 / 话题配置；发送前预算预留、调用后冻结、历史费用兼容。上下文压缩仍使用官方实现。
- 模型下线调用保护与替代模型解析；用户 VIP 模型访问保护的底层依赖。
- Gemini 3.8 Flash 定价覆盖，费用计算导出。
- 官方已传递话题 ID，浏览器及 Agent 传输无需重复回填。
- 错误分类识别数字 errorType/type 400/403，避免本地策略拒绝被误判为上游繁忙并重试。
- 利用官方 useBusinessErrorContent 扩展钩子，直接展示话题冻结与费用保护原因。

未在本阶段合并：全览及管理页面、完整模型展示管理、品牌与首页布局、图片展示优化、飞书资料连接器增强及其他展示 / 业务二开。不能将本阶段当作二开全量版本。

## 数据库迁移

旧副本有 166 条已应用记录，末尾为本地 0165。官方新增四条迁移的时间早于该记录，直接执行官方迁移器会跳过它们。

先运行 `python3 src/_custom/deploy/v2217/reconcile_clone.py`。脚本只允许操作 lingshu-v2217-pg /lobehub\_v2217\_acceptance，核对官方 SQL 哈希与 0157 基线，在一个事务内按哈希补齐缺失记录，保留旧记录的哈希和时间；重复执行跳过已应用条目。

随后执行官方迁移器，应用生成的 `0162_lingshu_minimal_access_cost`。这是新版分支的一条新迁移，时间晚于旧库最后记录；不是覆盖旧库已应用的同编号迁移。SQL 已按重复执行要求处理。新增表同时支持全新测试库；旧副本保留原有数据。

已验证：四条兼容迁移重复执行成功；旧 166 条历史全部保留；官方迁移器执行两次成功；副本共有 171 条记录。第三步应继续生成追加迁移，不覆盖任何已部署历史。

## 部署隔离

- chatdev → 127.0.0.1:3232 → lingshu-v2217-app-dev。
- 旧 chatdev 容器 cotti-v2216-native-app-1 保留在 3230，作为应用回退备份。
- chat.cotti.ai → 原 3231 /cotti-v2216-native-app-cotti-1，配置文件哈希未变。
- 独立 PG：lingshu-v2217-pg /lobehub\_v2217\_acceptance；独立持久卷，8GB 内存上限。
- 独立 Redis：lingshu-v2217-redis。
- 独立 QStash：lingshu-v2217-qstash，复用已安装 CLI 2.37.18 二进制，1GB 上限。未更改旧 QStash。
- 独立 S3：lingshu-v2217-storage /lobe-v2217，持久卷；通过 chatdev 的 /lobe-v2217/ 路径访问。
- 应用使用独立服务地址；出站接入既有应用网络。不存在新应用写入旧 PG、Redis、QStash 或 OSS 的配置。

副本不是持续同步库。切换后两个域名产生的数据各自独立。副本恢复时为 110 用户、1930 话题、30278 消息；另已补齐备份后至首次切换（2026-09-13 09:58:59 +08:00）之间的 7 条消息，并通过官方话题汇总函数重新计算费用。切换后旧版新建的话题不导入。本轮已复制 1,947 个历史文件及生成预览对象，共 18,832,937,684 字节；新附件也进入独立存储。原存储缺失的 1 张历史图片及 3 个服务商临时原始链接另行记录，不宣称零缺失。

## 验证记录

- 185 项登录、费用、模型策略等定向测试通过；全仓类型检查通过。
- 错误分类及费用提示额外 12 项测试通过，原分类器的反向验证有 2 项失败，证明回归测试有效。
- 原生构建成功；最终补丁镜像以交接记录中的实际 tag/digest 为准。
- Chrome 9222 已实际打开新版：标题 “首页・LobeHub”，保留用户既有登录状态。
- 专用话题 tpc\_GPIT4WaWf9Sy 的 Gemini 3.8 Flash 真实调用返回 “验收通过”，自动命名成功。
- 记录 774 Token / US$0.000959；旧数据库不存在该话题，验证了写入隔离。
- 临时将该验收话题额度设为 1 分钱，下一次请求被冻结，累计费用未增加。最终 r4 已实测立即拦截、不再重试；已删除临时额度与该测试冻结，恢复后真实问答返回 “恢复验证通过”（1064 Token / US$0.00189）。
- 独立 S3 实际 Put/Get/Delete 往返通过。
- 尚不能宣称所有 Agent 工具、定时任务、飞书资料、历史附件、分享及图片流程完成验收。

## 回退边界

Nginx 切换前配置保存于私有记录中的 chatdev.conf.before-v2217。回退应用入口可恢复该文件并经过 nginx -t 后 reload，但这会回到旧数据库，不能保留新版切换后新增的数据。必须先备份新版数据并明确数据处理方式，不能将简单切回旧镜像称为无损回滚。

## 当前验收镜像

- `lobehub:lingshu-v2217-minimal-20260913-r4`
- `sha256:d112dcfea41ca0840cd5ee4e85f32d3ff957f37876c0bd1ea4cfbb25a6c1bd40`
- 新旧三个应用容器均运行，重启计数为 0；仅 chatdev 的 Nginx 上游发生变化。
- 回退用旧 chatdev 保留在 3230，chat.cotti.ai 保留在 3231。
- 当前状态：最小版本已部署，第一轮自动补验与存储补齐已完成；邮箱重新登录已由用户完成并核对会话，待用户确认最小版本。不得提前将剩余二开合并视为已完成。

## 第一轮端到端补验（2026-09-13）

本轮保留最小二开范围，未开始第三步全量回填。私有截图与执行记录位于 `.records/v2217-minimal`，不发布到外部社区。

### 已复现并修复的部署问题

- `INTERNAL_APP_URL` 遗留旧容器 `cotti-v2216-native-app-1`，导致独立 QStash 无法投递任务完成回调。真实后台任务出现 operation 已 done、task 仍 running 的状态。
- 已改为 `http://lingshu-v2217-app-dev:3210` 并重建 chatdev 容器，镜像仍为 r4。旧 chat.cotti.ai 未修改。
- 对本轮自己的两个未收尾任务，通过官方完成回调补投递；新增定时任务经 QStash 调度、模型执行及官方回调成功，状态从 running 进入 scheduled。
- 本轮创建的 T-18 至 T-21 已全部暂停；不把 `maxExecutions=1` 配置本身当作自动停止已验证的证据。
- 新增 `python3 src/_custom/deploy/v2217/verify_runtime.py`，只读核查公网入口、内部完成回调、数据库、Redis、QStash、存储隔离，以及旧 cotti 配置哈希。

### 实测证据

| 项目     | 结果及边界                                                                                                                                                                    |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 云端沙箱 | 用户完成社区授权后，实际执行 Python、写入 `/tmp/acceptance.txt` 并通过独立读文件工具读取 `83810205`；`sandbox-round2.png`。授权前的 MARKET\_AUTH\_REQUIRED 保留为失败记录。   |
| 平台文档 | 官方文档工具创建 `acceptance.txt`，正文为计算结果；不是沙箱文件，不能用来替代沙箱验收。                                                                                       |
| 新附件   | 浏览器上传专用 TXT，文件写入独立存储；模型准确读取验收数字 `46281`；`upload-round2.png`。                                                                                     |
| PDF      | 官方生成 PDF 并显示中文预览，`pdf-round2.png`；另保存接口返回的 63,176 字节 PDF，核对 `%PDF-1.3` 文件头，并用 pdftotext 验证中文及两个验收数字。                              |
| 图片生成 | Nano Banana 2 单张真实生成成功，数据库异步任务 success，页面图片与缩略图可见；`image-round2.png`。                                                                            |
| 后台任务 | 真实 task.run/ 定时调度创建服务端 operation，Gemini 3.8 Flash 返回结果；修复内部回调后成功收尾，`task-round2.png`。                                                           |
| 历史分享 | 既有分享链接正文及历史图片均已复验，包含未登录窗口；`share-restored-round2.png` 与 `share-anonymous-settled.png`。后台标签页初始未渲染在置前后恢复，不能归为应用故障。        |
| 重新登录 | 用户完成 QQ 邮箱登录；独立窗口会话接口 200、已登录且匹配原 QQ 邮箱账号，进入首页。新 Gmail 注册邮件不作为验证码登录证据。                                                     |
| 历史附件 | 原 OSS 授权 HEAD 200 / 新存储 404 的差异已补齐。用户授权复制后，1,947 个对象复制并核验大小完成；64 MB PPT 抽样在两端均为 200 且大小一致；1 个原本缺失对象保留为历史数据缺口。 |

### 本阶段缺失与第三步范围

- 官方个人部署分享按钮进入导出，链接分享入口由 `enableBusinessFeatures` 控制；旧版新建分享入口需在第三步核对回填。

- 飞书资料连接器增强未回填，本阶段不能宣称其端到端通过；普通 / 蓝版飞书登录配置与该连接器是不同能力。

- GPT Image 2.5 Flare 展示适配、强制单张、隐藏视频、品牌及平台管理等仍在第三步范围，不能据原生页面将其标为已合并。

- 当前复制涵盖历史 files / 对应 global\_files 的对象，以及生成图片 / 视频的主文件、缩略图与主题封面引用；不把数据库恢复或文件数量当成完整存储校验。

- 任务页面的内置 task-agent 仍继承旧记录中的 deepseek-v4-pro，原生界面显示已禁用。它与实际运行任务时明确指定的 Gemini 3.8 Flash 是不同配置；后续需要核对系统助理默认模型，不能将任务执行成功外推为所有辅助聊天模型可用。

### 存储地区与访问控制补正

- 本地 RustFS 使用 `us-east-1`。继承原 OSS 的 `ap-southeast-1` 时，Bucket Policy 修改重复返回 503；同一操作改用 `us-east-1` 成功。已同步修正应用环境并重建容器，仍使用 r4 镜像。
- 新桶初始化时的匿名读策略已清空，保留 `S3_SET_ACL=0`，与原 OSS 的签名读取方式一致。授权 HEAD 200、匿名直接读取 403；平台 `/f` 附件在已登录浏览器读取 200 且内容正确，匿名访问跳转登录。
- 复制程序通过本机 S3 地址写入，绕开 Nginx 上传体积限制；支持按对象恢复，跳过大小核验一致的已完成对象，不覆盖大小不一致的目标对象。
- 对已复制的 5 个对象分别从原 OSS 与新存储读取并计算 SHA-256，5 项全部一致；全量对象执行大小核验，不宣称全量哈希核验。
- 已发现一张 2026-03-23 历史图片在原 OSS 与更早本地存储均 404，数据库同一 hash 无其他引用。保留记录，不删除、不伪造替代文件；具体路径仅保存在私有迁移记录。
- 原生搜索接口也已验证：验收话题标题搜索与数字 `46281` 消息搜索均返回 200，并命中对应记录。

本轮验收未新增应用功能代码；增加部署只读核查脚本及本记录，修正运行配置。新旧入口保持分离，四个测试任务均暂停，无运行中模型任务。下一步由用户确认最小版本，之后才合并剩余二开。

2026-09-13 邮箱登录补验：用户确认 QQ 邮箱登录成功；只读浏览器探针确认原账号已登录且进入首页，证据为私有记录 email-login-complete.json/email-login-success.png。未关联或合并 Gmail 新注册账号。历史源图片缺口仍保留，不能将本轮结果表述为历史数据零缺失。
