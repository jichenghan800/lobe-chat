# Astraflow 压测组（2026-09-14）

## 当前范围

- chatdev.cotticoffee.com 与 chat.cotti.ai 共享用户组和成员配置；本轮不修改 chat.cotticoffee.com 生产服务器。
- Astraflow 复用已有 `openai` 渠道及其服务端 Key，接口为 `https://api-sg.umodelverse.ai/v1`。
- 创建空的 “压测组”（`astraflow-pressure`），不移动现有用户。管理员在人员管理 “用户组” 列分配，现有 VIP、Agent、单话题金额权限保持独立。
- 组内聊天模型：Gemini 3.8 Flash、Gemini 3.5 Flash Lite、GPT-5.6 Terra、GPT-5.6 Sol；图片模型：GPT Image 2.5 Flare。
- 对话和辅助任务默认使用 Gemini 3.5 Flash Lite，智能默认使用 Gemini 3.8 Flash。已有话题若引用组外渠道，由服务端拒绝；客户端重新取得组内模型配置后可选择组内模型，不承诺自动迁移历史任务。

## 实际二开

- 迁移 0165 新增 `cotti_user_groups`，用户策略增加可空 `group_id` 外键。不写成员回填，不改业务消息和历史费用。
- 用户组模型配置独立于平台全局模型配置。压测组的 Sol 仅绑定 `openai`，Azure 等原渠道的下线与替代规则保持原状。
- 注册为组专属的渠道对未分组用户不可用；组内只允许声明的模型。禁用组仍保留渠道边界，不退回普通权限。
- 运行时在聊天、结构化输出、图片、视频、向量接口调用前检查最新组权限；组成员使用服务端渠道连接，忽略个人保存的连接覆盖。
- 普通模型选择、图片列表和默认模型使用用户组配置；用户设置读取时覆盖辅助模型的有效配置，不改用户原有偏好记录。
- 人员管理保留单行布局，新增即时保存的用户组选择。全局模型管理使用独立的管理员配置查询，避免把组内配置写回全局。
- 首轮组定义在 `src/_custom/registry/astraflowPressureGroup.ts`；人员分配已提供界面，本轮不新增通用用户组创建和模型配置编辑页面。

## 定价与验收边界

- 五个型号均在 Astraflow `/models` 返回目录中；本地模型解析识别为四个聊天模型、一个图片模型，均有价格元数据。
- 价格沿用现有平台估算，不等于 Astraflow 的逐笔成交金额。尚未新增交易明细导入、请求级费用账本、历史用户组费用快照或自动账单匹配。
- 下一步应使用分配后的专用测试账号做少量真实请求，核对请求标识、Token、缓存、平台估价和 Astraflow 扣费；无话题辅助调用要单列，不能归入无关话题。
- 用户组与用户策略数据库测试、接口权限 / 模型显示测试、辅助模型回归、类型检查和正式构建通过。数据库变更在共享库内以事务回滚演练，确认临时对象已清理。
- 不把模型目录确认或自动化测试表述为五个模型都已完成真实生成、浏览器端到端或费用核账验收。

## 部署与回退

- 镜像：`lobehub:lingshu-v2217-astraflow-group-20260914-r1`。
- 发布前保留共享数据库 dump、容器环境和旧应用。备份目录：`/opt/backups/lingshu-astraflow-group-20260914/`。
- 先暂停共享调度并确认无运行任务，停止两入口；执行镜像自带迁移器、创建空组，再以五模型环境配置启动新应用。两入口使用同一数据库、存储和队列。
- 旧容器：`lingshu-dev-before-astraflow-20260914`、`lingshu-cotti-before-astraflow-20260914`，停止且禁用自启。
- 回退必须同时恢复旧镜像与旧模型目录，保留新增数据；不能只回退代码却保留开放目录，也不能用旧 dump 覆盖新增用户或消息。
- 私有执行与检查记录：`.records/astraflow-pressure-group/`。环境及 Key 不提交代码库。
