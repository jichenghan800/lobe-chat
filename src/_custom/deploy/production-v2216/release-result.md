# 2026-09-11 生产发布验收记录

目标：`https://chat.cotticoffee.com`，47.236.135.3 / 10.82.70.138。用户在发布准备完成后明确批准切换。

## 版本与数据

- 首次 v2.2.16 切换完成于北京时间 17:13，应用源码 `9ea2e69c04`。
- 预算 TTL 兼容补丁源码 `c80506d9ed`；镜像 `lobehub:lingshu-v2216-production-budget-ttl-20260911`。
- 补丁应用切换完成于北京时间 17:28，运行环境变量逐项比对未变化。
- 最终镜像 ID：`sha256:6a66c816202872abb513be013055dbe5d43d8c7a464e5ea3de5334a8517bbb72`。
- 停写后最终备份：生产 `/opt/lobechat-releases/v2216-20260911/cutover-backup-20260911-171054/`；另已复制到本机私有记录目录。
- 从最终 dump 实际解析得到：475 用户、6,217 话题、87,664 消息。早期演练备份之后增加 1 话题、44 消息，属于准备期间原生产新增数据；不是迁移生成。
- 迁移后恢复服务，消息继续增加。保留生产原账户、模型凭据、飞书、SMTP、对象存储和域名配置，不复制开发数据库。
- 数据库启动时间仍为 6 月 26 日，未重建。QStash 未主动重建，但观察期间发生一次内存上限触发的自动重启，处理见下。

## 已验证

- 公网 HTTP 200；无登录时转本域名 signin，无 chatdev 重定向。
- Chrome 9222 原管理员会话仍有效；首页、概览实际截图已检查，历史话题可见。
- 管理权限、模型配置、平台统计、全览列表、人员管理、话题额度接口均 200；默认额度返回开启 / 1000 分。
- Sol 的 Azure Agent 和话题引用清零；保留历史消息实际模型与费用。
- 使用专门的发布验收话题进行小额真实回复：Gemini 3.8 Flash、Azure GPT Terra、Gemini 3.5 Flash Lite 均成功。页面累计 5,072 Token、约 ¥0.0894；数据库使用信息与页面对应。测试话题保留用于验收。
- 既有 dispatcher ID 复用（每 10 分钟）及新增 goal sweep（每 5 分钟），均指向生产域名，无重复 dispatcher。
- 社区推荐曾出现上游 403；既有 WARP 连接、目标 IP 集合及生产网络匹配正常。复测社区推荐接口 200，本次未调整 WARP 配置。
- 老页面短暂调用已移除的 Agent 入口接口；刷新新版页面后本次观察不再出现该错误。
- 预算补丁 11 项回归测试、完整类型检查、镜像构建通过。仅预估时将缓存 TTL 查价取已列出的最高价，不改变实际计费或关闭保护。

## QStash 内存事件

17:28:35 内核记录 QStash 容器内存 cgroup OOM，杀死约 489 MiB RSS 的 qstash-cli；原限制 512 MiB，容器自动重启一次，恰好导致应用启动时两条调度登记失败。将限制在线提高至 1 GiB（未再次重启），并写入生产 Compose；两条调度重新登记均返回 201。不得将该事件归因于整个主机内存不足。

提高限制避免立即再次触顶，但不能证明不存在长期内存增长。当前 QStash 是 CLI 自托管实例，需继续观察内存和自然调度；重启期间在途消息是否受影响尚未全面核实。原配置快照保留在 `compose-before-qstash-memory.json`。如回滚旧应用，旧 Compose 中 QStash 上限仍是 512 MiB，应保留本次 1 GiB 配置，避免后续全量重建时退回。

## 验收范围与后续

现有管理员登录验证不等于完成一次新的普通飞书、蓝版飞书或邮箱验证码登录。新登录流程留待用户协助单独验收；本次没有向其他人员发送邮件或消息。QStash 已核对登记状态，未强制执行客户定时任务；需在自然触发时继续观察执行和重试情况。附件上传 / PDF / 分享及成本冻结边界的完整业务回归不在本次小额冒烟范围内。

下一步观察重启、错误、费用保护误拦截及实际任务执行，完成新登录和附件回归。准备阶段已验证旧镜像连接升级库启动及关键读取；如需回滚，沿用 README 的旧版回滚命令，保留升级后的数据库和新增写入。预算补丁前 Compose 另存于生产 `compose-before-ttl.yml`。

发布日志、备份、截图、接口结果均在 `.records/production-release-20260911/` 私有目录；不上传包含用户数据的证据到第三方验收平台。
