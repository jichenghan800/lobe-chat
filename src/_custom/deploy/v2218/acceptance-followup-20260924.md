# v2.2.18 登录后验收及显示修正

本轮只涉及 chatdev.cotticoffee.com 与 chat.cotti.ai；生产未操作。

## 已取得的真实结果

- 用户完成 chat.cotti.ai 登录，两域名身份一致、共享话题；自建沙箱验收附件经第二域名授权下载 HTTP 200，内容一致。
- 云端沙箱新话题 `tpc_2tUMVdc8Wr5w`：Python 写入、读取核对、导出成功；下载 12 字节，内容为 `CLOUD2218_OK` 加换行。验证的是客户端工具执行链。
- 自建沙箱测试话题 `tpc_3vspPTHTcWrR` 临时设置 ¥0.01 后发送续问，发送前预算拦截成功，无新增模型用量。浏览器显示费用原因和查看历史、新话题、管理员解冻入口。随后已恢复继承额度并解冻，数据库确认无额度覆盖、无冻结行。
- 飞书新话题 `tpc_02LQygX0fETM` 未通过：模型走了网页抓取而非连接器，不能凭其回答判断授权。随后在当前管理员浏览器会话直接执行只读 fetch-doc，飞书明确返回 `user access token is invalid or expired`。需要重新授权后再验收，不将此项记为通过。

## 两个最小修正

1. 全览管理面板原先调用 SWR 全局 mutate，无法刷新页面的独立缓存。改用当前 SWRConfig 的 mutate，冻结、解冻、额度保存后刷新列表与详情。逻辑隔离在 `useRefreshTopicOverview.ts`。
2. 全览列表及管理详情使用统一 SQL 条件：明确的 `TOPIC_COST_FROZEN` 发送前拒绝、且没有任何用量字段时，不作为缺失计费记录。保留有用量的错误、其他未知错误和原有金额汇总，不补造费用，不改预算保护。

回归：模拟旧行为两项测试失败；修正后 2 文件 7 项通过，相关文件 lint 通过，全量类型检查通过。测试覆盖独立缓存的冻结状态刷新、工作区键、预算拒绝与真实用量共存、未知错误仍标为缺失计费。

运行证据留在本机 `.records/v2218-cutover/`、`.records/v2218-display-fixes/`，不上传官方云。完整端到端验收仍需覆盖服务端委派、自动任务实际回调、大附件上传进度及飞书重新授权后的读取；不得据本轮局部通过宣称全部验收完成。

## 发布及浏览器复测

两个开发入口已同步发布 `lobehub:lingshu-v2218-full-acceptance-20260924-r2`，镜像 ID `sha256:823336c14091938052303050ee6bf68054c3a6f2d054ba767a74f19775e85df9`。同一当前数据库保持不变，无 schema 或用户数据迁移；停写备份与上一版容器配置位于 `/opt/backups/lingshu-v2218-display-20260924-r2`。上一版两个容器停止且禁用自启。

部署后浏览器验证冻结与解冻两个方向：列表及右侧状态无需刷新即一致；累计费用仍 ¥0.18，计费记录恢复 3/3，不再显示费用不完整。测试话题最终未冻结、额度继承，数据库复核一致。chat.cotti.ai 刷新后登录保留、云端沙箱附件下载 HTTP 200 且内容一致。两项 QStash 计划已恢复且未重复，运行校验 8 项全部通过。相关查询与筛选补充测试 3 文件 14 项通过。

后续授权及剩余核心链路已于同日补测，结果见 [第三轮集中验收](acceptance-round3-20260924.md)。上文授权过期等失败记录保留为当时的真实结果。
