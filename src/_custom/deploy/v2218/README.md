# v2.2.18 隔离升级准备（2026-09-24）

## 基线和部署边界

- 官方基线：v2.2.18，14dfc07b14。
- 分支：upgrade/v2.2.18-minimal；工作树：/opt/lobechat-v2.2.18。
- 已推送回退标签：milestone/v2.2.17-before-v2218-20260924，指向 313d0beb75。
- 三个现有域名仍运行 v2.2.17 image-fit-20260923-r1；没有重启应用、切换域名或修改原数据库。
- 私有备份目录：/opt/backups/lingshu-v2218-isolation-20260924，权限 0700，包含开发、生产数据库归档、SHA256 和运行配置，不提交 Git。
- 备份为在线一致性快照；正式切换前必须重新备份，保留后续业务写入。

## 隔离数据库验证

副本容器 lingshu-v2218-development-verify-pg 和 lingshu-v2218-production-verify-pg 使用与线上相同的固定 ParadeDB PG17 镜像。各自独立卷、network=none、无宿主端口、2 CPU / 4 GiB。只运行数据库，不运行应用、模型、邮件或任务。原 OSS 未修改，附件对象尚未复制。

两份备份均经 pg\_restore --exit-on-error 完整恢复，再执行官方五条新迁移。重复执行缺失列表为空，原迁移哈希全部保留。以下业务表行数迁移前后不变：

| 副本 |  用户 |    话题 |     消息 |  附件记录 |    助理 | 迁移记录前 → 后 |
| -- | --: | ----: | -----: | ----: | ----: | --------: |
| 开发 | 118 | 2,101 | 33,480 | 2,544 |   301 | 174 → 179 |
| 生产 | 488 | 6,502 | 91,370 | 9,797 | 1,395 | 162 → 167 |

验证限于 SQL 执行、历史迁移保留、重复执行无操作和上述行数一致，不代表全部字段逐值一致或应用验收通过。本机详细记录位于 .records/v2218-preparation/。

## 迁移策略

保持官方 journal 和 SQL 原样，不覆盖已上线的二开 0162–0165 迁移历史。reconcile\_copies.py 按固定 SQL SHA256，在事务、public search\_path 和 advisory lock 内补齐官方 0162–0166；再次运行按哈希跳过，避免非幂等 DDL 重放。

脚本只接受 development /production 两个副本标识，检查容器无网络、无端口及数据库名称。默认仅列出计划，--execute 才执行。禁止改指向线上。

这仅解决现有副本的升级前补齐。后续须按新版 schema 生成追加二开迁移，验证全新安装、官方自动迁移器及旧应用回退兼容，不能手工覆盖已发布迁移。

## 最小二开范围与下一步

保留真实用户验收必需的登录和准入、普通与蓝版飞书、邮件验证码、用户组模型渠道权限、模型下线限制、金额保护和计费字段。不能默认放开模型权限或停用费用保护，不恢复旧上下文硬阈值。

初期沿用既有执行路径，不同时引入 Gateway 架构或 Docker 升级。先验证原生工具装配、官方沙箱与文件读写；OnlyBoxes、管理和全览等剩余二开按源码覆盖清单在后续阶段合并。

尚未完成：最小源码适配、依赖安装与构建、独立应用环境、附件存储与登录回调、官方迁移器及新库安装、回退兼容、Chrome 产品验收、剩余二开合并、域名切换。

本次 Python 脚本通过语法检查及两个副本实测，JSON 使用既有工具格式化。隔离工作树未安装依赖，标准提交钩子无法解析依赖，本次文档和脚本提交跳过钩子；不能视为应用构建或全量测试通过。
