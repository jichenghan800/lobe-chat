# v2.2.18 隔离升级（2026-09-24）

## 范围与状态

按用户最新决定，一次合并全部必要二开，统一测试验收，不再逐批请求验收。官方基线为 `14dfc07b14`；二开基线为 `313d0beb75`，已推送回退标签 `milestone/v2.2.17-before-v2218-20260924`。

工作树 `/opt/lobechat-v2.2.18`，完整候选分支 `upgrade/v2.2.18-customizations`。早期 `upgrade/v2.2.18-minimal` 仅为登录适配检查点，不可作为完整服务发布。

2026-09-24 经用户授权，开发双入口已同步切换至 v2.2.18，共享新验收数据库；生产 chat.cotticoffee.com 保持 v2.2.17。正式切换记录见 [双入口验收发布](acceptance-release-20260924.md)。

## 备份与隔离

私有目录 `/opt/backups/lingshu-v2218-isolation-20260924` 权限 0700，存放开发、生产在线一致性数据库快照、SHA256 和运行配置，不提交 Git。正式切换前需重新备份并处理后续业务写入。

两个副本容器 `lingshu-v2218-development-verify-pg` 和 `lingshu-v2218-production-verify-pg` 各自独立卷，network=none，无宿主端口，2 CPU / 4 GiB，使用固定 ParadeDB PG17 镜像。原 OSS 未修改；独立附件存储与真实登录回调尚待配置验收。

| 副本 | 用户 |  话题 |   消息 | 附件记录 |  助理 |
| ---- | ---: | ----: | -----: | -------: | ----: |
| 开发 |  118 | 2,101 | 33,480 |    2,544 |   301 |
| 生产 |  488 | 6,502 | 91,370 |    9,797 | 1,395 |

以上业务表行数在官方迁移前后不变，不代表全部字段逐值比对通过。

## 数据库迁移

官方历史 SQL 不改写。`reconcile_copies.py` 按固定 SHA256 补齐官方 0162–0166，保留旧二开迁移记录。脚本仅接受指定无网络副本，默认预览，`--execute` 才执行，禁止改指向线上。

新追加 `0167_lingshu_v2218_customizations.sql` 与 schema 快照覆盖 14 张二开表，补齐索引和外键。在两份副本上分别执行两次，原二开表行数不变；官方自动迁移器随后在两份副本上各执行两次，均成功。

空库全新安装发现官方历史迁移引用尚未创建的 `public.tasks`；用未修改的官方 v2.2.18 迁移复现同样失败。此项为上游首次建库问题，不通过修改历史 SQL 掩盖。现有数据库升级路径已通过；空库安装和旧应用回退的产品兼容性不标为通过。

## 构建与依赖

运行 `build.sh` 构建候选，Dockerfile 使用固定现有运行镜像作为基础，替换应用产物。候选镜像与测试证据见 [合并状态](../../upgrade/v2.2.18-merge-status.md)。

依赖安装未运行第三方安装脚本。官方工作区默认关闭锁文件，本轮显式生成并私有备份 `pnpm-lock.yaml`，SHA256 为 `a5e8b9da36e5b8448dafbaf6aab0633cad5ff04fa34ffd52094e61e2ed3f5688`；后续复现使用 `--lockfile=true --frozen-lockfile`。

离线启动检查使用副本网络命名空间、随机测试认证密钥、不可达存储占位配置，不加载真实邮件、模型或 QStash 凭据。它仅证明应用启动与静态资源可达，不代表真实登录、附件和任务验收完成。

## 发布前仍需完成

统一 Chrome 验收覆盖登录、模型渠道与权限、金额控制、管理全览、上传下载、官方 / 自建沙箱及委派工具。配置隔离附件、回调和任务副作用控制后再开展真实业务测试。开发双入口切换的最终备份、静态资源归档和启动验证已完成；实际浏览器验收结果以发布记录为准。生产切换与带新写入的回退演练仍待后续安排。

2026-09-24 登录后补充验收及 r2 显示修正已同步发布，见 [后续记录](acceptance-followup-20260924.md)。
