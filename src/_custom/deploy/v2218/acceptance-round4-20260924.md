# v2.2.18 第四轮集中验收

2026-09-24。双开发入口维持 r2 镜像与共享数据库；生产未修改。本轮无业务代码改动。私有证据在 `.records/v2218-round4/`，不提交用户数据或运行密钥。

## 分享与 PDF

- 使用管理员自有测试话题，公开分享后，两个开发域名的匿名页面均可读取同一对话，附件下载内容与测试标记一致。
- 首次匿名页面加载曾长时间等待；非批量分享 API 与源站探测正常，刷新后渲染成功。原因未确定，不标为已修复。
- 已恢复话题私有，数据库确认 private，匿名分享 API 返回 403，浏览器显示 “此分享为私密状态，无法访问”，截图已查看。
- **附件直链不随分享撤销。** 匿名浏览器禁用缓存请求仍重定向；不带认证的源站 `/f/文件ID` 也返回 302。当前路由与官方 v2.2.18 无差异，明确按文件 ID 匿名生成下载地址。故不是浏览器缓存或本次二开新增问题；不能把话题关闭分享描述为附件撤权。是否改造作为独立范围问题待用户决定，本轮未改接口。
- PDF 通过真实按钮生成并下载，文件 71,792 字节，PDF 头与中文、测试标记核验通过，预览截图已查看。排版仍有 Markdown 标记和部分 emoji 方框，只标记基本生成下载通过，不标记富文本质量通过。

## 带新写入的隔离回退

从当前 v2.2.18 数据库新建备份，仅恢复到无网络、无宿主端口的独立 PG；未覆盖在线数据库。私有备份目录 `/opt/backups/lingshu-v2218-rollback-probe-20260924` 权限 0700。

1. 旧版镜像启动、自动迁移和认证健康端点通过。
2. 旧版实际 TopicModel / MessageModel 读取新版新增话题的 4 条根消息；在副本新建话题并写入、读回测试消息通过。
3. 同一副本重新启动新版镜像，迁移与健康端点通过；新版实际模型读取旧版刚写入的消息，内容匹配。
4. 迁移记录数前后均 180；在线库不存在副本测试话题。临时旧版、新版应用与 PG 均已停止；备份保留。

结论仅覆盖启动和话题 / 消息基本读写兼容，不代表旧版所有业务功能或外部服务端到端兼容。测试网络隔离，没有模型、邮件、OSS 或任务外发。

## 当前交付边界

双入口运行校验 8/8 通过。核心链路结果见前三轮记录；本轮补齐基本分享、PDF 与数据库副本回退。仍未覆盖全局调度扫描完整周期，附件撤权策略待确认，PDF 排版与分享首次加载等待已记录。生产升级尚未执行，不能宣称全量验收无保留通过。
