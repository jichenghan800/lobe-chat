# v2.2.18 第三轮集中验收

2026-09-24。沿用双开发入口 r2 镜像，无业务代码或生产发布。证据保存在本机 `.records/v2218-round3/`，不上传外部验收平台。

## 结果

| 场景                        | 实际证据                                                                                                                                                                                               | 结果 |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| 飞书资料重新授权            | 用户在可见 Chrome 完成授权；原有连接器更新令牌，当前登录会话直接只读调用 fetch-doc 返回 HTTP 200                                                                                                       | 通过 |
| 对话选择飞书资料            | 通过 @ 菜单选择连接器，新话题 `tpc_TQDQqPV8pFlH` 实际调用 `feishu-documents/fetch-doc`，只取指定文档前 200 字符，正确回复标题                                                                          | 通过 |
| QStash 回调和服务端自建沙箱 | 管理员私有任务 T-23，通过现有 QStash 投递一次正常签名的 schedule-execute 回调，`tpc_AZWDfaGmBQIi` 服务端 executeCode 写入读回 `SCHEDULE2218_OK`，话题元数据为 onlyboxes                                | 通过 |
| 单次调度限额                | T-23 设置 maxExecutions=1；最终任务 completed，task\_topics 仅 1 条 schedule 执行，未新增全局计划或修改扫描频率                                                                                        | 通过 |
| 浏览器委派沙箱              | `tpc_Iha6Li57VpSM` 的 callSubAgent 创建独立线程，子线程 executeCode 返回 `VERIFICATION_SUCCESS: DELEGATE2218_OK`                                                                                       | 通过 |
| 服务端委派沙箱              | 单独私有任务 T-24，`tpc_MSGkavIS0XzV` 的父、子两条 agent\_operations 均 done，子线程真实执行 Python，返回 `SERVER_DELEGATE2218_OK` 并确认一致；任务运行 completed，非自动任务最终 paused，不会反复执行 | 通过 |
| 大 Excel 上传               | 浏览器生成只含测试文本的有效 XLSX（14.5 万行、86,199,709 字节，约 82.2 MiB），通过文件输入事件走真实上传流程                                                                                           | 通过 |
| 上传进度与完成              | DOM 记录 4%、14%、24%、34%、46%、55%、65%、75%、82%、90%、99%，之后显示 “正在完成上传” 再完成；截图已查看                                                                                              | 通过 |
| Excel 存储与下载            | file\_WI13ruookNTi 入库大小一致，file\_chunks 为 0；授权 GET Range 返回 206，前四字节为 ZIP 头。未发送给模型分析，已移除输入框中的测试附件                                                             | 通过 |

大文件测试使用浏览器 File/DataTransfer 设置文件输入事件，验证真实前端处理、上传请求、进度与存储；不声称覆盖 Windows 原生文件选择器。HEAD 探测遇到预签名方法不匹配的 403，随后使用 GET Range 正常；不把 HEAD 探测失败归因上传失败。未执行整文件下载哈希对比或全部 Excel 解析。

本次四个新测试话题费用分别为 ¥0.1734、¥0.2389、¥0.3755、¥0.3093，合计预估 ¥1.0971；助手回复计费记录分别 2/2、2/2、4/4、4/4。委派结束瞬间浏览器曾显示未收齐的费用，刷新后完整，与持久化记录一致；本轮未修改计费逻辑。

## 边界与后续

- 本轮 QStash 验证的是单个任务的真实签名回调与执行，不宣称另行覆盖 10 分钟全局扫描器的完整自动触发周期。
- 本轮飞书验证是显式选择连接器后的只读文档链路，不含写入、聊天检索和未选择工具时的意图识别。
- 未新增用户授权范围，未改其他用户的话题和任务；测试任务不再运行。当前双入口运行校验 8 项通过，生产未修改。
- 后续补齐分享 / PDF 导出等外围回归，并在隔离副本验证带新增数据的应用回退；最终生产切换尚未安排，不能据本轮通过宣称生产升级完成。
