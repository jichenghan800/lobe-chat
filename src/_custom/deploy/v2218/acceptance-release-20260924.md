# v2.2.18 开发双入口验收发布

2026-09-24 11:26（北京时间），按用户 “两个域名可以切换作为验收环境” 的授权完成。源码合并提交 `94a80e49b2`，分支 `upgrade/v2.2.18-customizations`。

## 部署与数据

- `chatdev.cotticoffee.com` 与 `chat.cotti.ai` 同步使用 `lobehub:lingshu-v2218-full-acceptance-20260924-r1`，镜像 ID `sha256:f782ed8c53a70bc6d2eae32490ad4edb4e0aeb4e9d0c6e485ecb111a7aec13d5`。
- 应用容器名、3232/3231 端口、各自认证回调及网络优先级保留。原生业务数据库使用同一 PG 内的新数据库 `lobehub_v2218_acceptance`；两个入口指向完全相同的数据库 URL。原 `lobehub_v2217_acceptance` 保留。
- 继续共享现有 OSS、Redis、QStash 与 OnlyBoxes。未复制、移动或删除旧附件对象；测试只新增管理员验收文件。数据库隔离不等于附件存储副本隔离。
- 生产 `chat.cotticoffee.com` 未修改，继续 v2.2.17。

两个入口同时进入维护状态，暂停原有两项调度、确认无运行中 Agent 操作后停止旧应用。最终停写快照通过 pg\_restore 列表检查，完整恢复到新数据库。补齐固定哈希的官方 0162–0166 后，由新应用自动迁移器应用追加二开迁移，总记录数 180。

迁移前后：118 用户、2,102 话题、33,491 消息、2,547 附件记录、301 助理，数量一致。两应用健康后一起开放入口并恢复原两项调度，未新增重复计划。模型展示、用户组、用户权限、话题预算及沙箱设置五张表的完整行指纹与原库一致。

原镜像公开静态 assets/vendor/i18n 已归档到 `/opt/lingshu-static-archive`；两域名原资源逐字节 SHA256 一致，缺失资源保持 404/no-store。没有归档 HTML、环境配置或密钥。chat.cotti.ai 源站证书为独立信任链，回环源站资源检查不作为公网 TLS 验证；公网 Chrome 已正常进入其统一登录页。

## 本轮浏览器与真实请求

已使用远程 Chrome DevTools MCP 并查看截图：

- chatdev 管理员现有登录保留，首页品牌、推荐与三模式入口可用。
- 默认组 / 压测组模型管理及 Astraflow 标识；关闭 Terra 弹出组内下线预览，显示影响范围，取消后未改配置。
- 人员管理一行式权限、用户组、额度展示；沙箱容量 5、测试时占用 1，配置保留。
- 全览全部 / 最近更新；输入另一域名的话题链接可定位历史话题，显示模式、费用和活动，刷新保留详情。
- 新管理员话题 `tpc_GP4s4dQIn8JD`：选择快速模型，实际消息为 `vertexai/gemini-3.5-flash-lite`；回复 `V2218_CHAT_OK`，654 Token，USD 0.000217，页面预估 ¥0.0015。
- 新管理员话题 `tpc_3vspPTHTcWrR`：自建沙箱 Python 计算、写入、读取、相对路径导出成功；授权下载 HTTP 200，内容严格为 `V2218_OK\n56`，11 字节。实际模型 `vertexai/gemini-3.8-flash`，页面累计约 6.4 万 Token、¥0.1848。工具记录及话题 onlyboxes 元数据已核实。本次走客户端工具执行链，不宣称覆盖服务端委派 / 队列路径。
- 飞书资料连接器及工具清单可见，但未执行飞书文档写入。

## 未完成的验收边界

chat.cotti.ai 登录后操作待用户完成统一登录。新邮箱 / 飞书登录、官方云端沙箱、服务端委派 Agent、自动任务实际回调、费用触顶冻结 / 解冻、上传进度及完整附件类型回归尚未在本轮全部测试。自动测试已通过不等于这些端到端用例通过；本候选仍用于集中验收，不是生产升级完成。

私有截图、步骤及逐项结果：`.records/v2218-cutover/`，不向官方云上传。测试中远程输入工具一次超时并继续输入，待输入结束后按回车完成链接检索；不将工具超时直接归因产品。

## 备份与回退

私有备份 `/opt/backups/lingshu-v2218-cutover-20260924`（0700）保留停写 dump、哈希、原容器配置和 Nginx 配置。旧应用 `lingshu-dev-before-v2218-20260924`、`lingshu-cotti-before-v2218-20260924` 已停止并禁用自启。维护配置已恢复。

**开放验收后新库已有新增写入，禁止直接启动连接旧库的备份容器回退。** 如需应用回退，应先暂停双入口和调度，备份当前新库，在副本验证旧应用兼容新 schema，再让两旧版应用共同连接当前数据库；不得用旧 dump 覆盖验收后的数据。该带新写入的回退尚未演练通过。

运行 `python3 src/_custom/deploy/v2218/verify_runtime.py` 可只读核对镜像、共享后端、域名和接口健康。旧 v2217 验证脚本固定旧库名，不适用于本次候选。
