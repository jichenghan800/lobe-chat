# 2026-09-20 生产发布记录

目标仅为 chat.cotticoffee.com（10.82.70.138 / 47.236.135.3）。
chatdev.cotticoffee.com 和 chat.cotti.ai 已运行候选版本，不在本次切换范围。

## 固定版本

- 发布前生产：`lobehub:lingshu-v2217-home-fixes-20260913-r1`，digest `sha256:4161f5ee5a429af8bc56623a5acfe79f27ee400ff67f33489642e090571e6a2a`。
- 当前生产：`lobehub:lingshu-v2217-delegated-sandbox-20260920-r1`，digest `sha256:261b66eb11009f4a522a17dd518964bc1e6b32a39d93013820f9916807d5778c`。
- 应用代码提交 `5fefe5f4bc`；同一镜像已在双开发入口部署。本目录的后续文档 / 发布脚本提交不改变镜像。
- 2026-09-20 12:34（北京时间）正式切换完成，生产已运行上述镜像。

## 范围与保留项

包含 9 月 13 日以来的全览、用户组、模型权限与计费、附件发送、沙箱选择与委派、冻结提示及换题判断修复，不仅是 9 月 20 日的五次提交。

完整保留生产数据库、OSS、认证、模型渠道、Redis、QStash 以及运行时环境。默认话题额度仍为 ¥15；开发环境的用户组、渠道配置和名额不复制到生产。
OnlyBoxes 在生产未启用，继续使用官方云端沙箱。现有 1 条上下文冻结、16 条人工冻结及 3 条预算冻结不在准备阶段改动；实际切换前重新核对，不能将快照数量当作实时值。

供应商履约台账 Skill 的业务脚本错误由用户修改 Skill；本次平台发布不包含该业务修复，不宣称错误文件已纠正。

## 数据库与回退演练

- 生产在线一致性备份约 381 MB，已在开发主机内部隔离网络完整恢复；备份 SHA256 已记录。
- 备份包含 478 用户、6347 话题、89169 消息、9517 文件、1378 Agent。这是备份时的快照，正式切换仍须停应用后重新备份。
- 迁移 0164（沙箱名额）与 0165（用户组）由镜像官方启动迁移器执行。时间戳晚于生产已执行迁移，不再使用 9 月 13 日的 reconcile.sql。
- 首次新版启动、重复新版启动、旧版连接升级后副本均通过；迁移记录 160 → 162，重复启动仍为 162。
- 上述三轮核心记录数量和模型、权限、预算配置摘要一致。对 user policy 比较排除本次新增、默认 NULL 的 group\_id；新用户组为空，已有用户没有被分配到新组。
- 隔离副本用独立临时测试管理员（非真实用户身份）检查新旧版本各六项管理 API：管理员权限、模型管理、统计概览、话题预算、全览列表、人员管理。12 项全部返回 200 且业务结果成功，测试账号及会话已删除。
- 隔离 Redis 独立；网络为 internal，QStash、邮件、Market 指向不可达地址。没有真实模型调用、消息发送或 OSS 写入。演练不代表生产 OAuth、模型生成及沙箱导出端到端验收。
- 演练初期修正 Docker 默认网段不足、PG 初始化就绪判断与镜像预置 schema 冲突、隔离 Redis 缺失；最终完整恢复及三轮启动通过。未修改应用代码解决这些环境问题。

## 切换包

生产私有目录 `/opt/lobechat-releases/v2217-20260920` 保存固定镜像、Compose、配置摘要、调度快照及验收结果。源码只保留无凭据的发布脚本。执行前默认只读预检：

```bash
python3 /opt/lobechat-releases/v2217-20260920/release.py
```

正式切换须在本轮准备结果确认后执行：

```bash
python3 /opt/lobechat-releases/v2217-20260920/release.py --execute
```

脚本校验镜像、旧容器、环境、Compose、依赖容器和队列定义；存在运行任务或近期可能未完成的流式响应时拒绝切换。停止应用后另做数据库 / 配置备份，再仅重建应用，保留依赖；启动执行迁移，验证 162 条记录、环境和预期调度定义，失败尝试恢复旧应用。

回退只切应用，保留升级后的数据库和新增业务数据：

```bash
python3 /opt/lobechat-releases/v2217-20260920/release.py --rollback --execute
```

## 切换后必须核对

生产首页 / 登录回调不跳开发域名；全览搜索与详情、用户组 / 模型开关、冻结弹框及现有额度；用户正常登录会话下的管理接口；一次获准的小量模型调用、附件读取及官方沙箱文件导出。没有完成真实业务验收前，不把接口健康等同于用户任务成功。

私有证据在开发主机 `.records/production-v2217-20260920/`，不得提交数据库、配置、会话或真实用户内容。

## 发布校验补充：新增任务扫描计划

生产原来只有 `lobe-goal-sweep`；候选镜像启动时还会注册源码声明的 `lobe-task-schedule-dispatch`，每 10 分钟回调生产 `/api/workflows/task/schedule-dispatch`。第一轮切换因旧发布脚本要求调度列表完全不变而回退，应用启动、162 条迁移、环境及依赖检查均已通过，数据库未回滚。

修正仅位于本目录发布脚本：允许这一项预期新增，仍拒绝开发域名回调、删除已有计划或增加其他计划。5 项回归通过；复用旧比较逻辑时预期新增场景失败。重新准备包时记录回退后容器和配置的新摘要，不绕过漂移检查。

实际切换使用私有 `execute-with-queue.py` 包装器临时暂停现有活跃任务扫描，结束时恢复。新版启动负责注册所需计划；切换后必须核对两个计划均启用。

## 正式发布结果（2026-09-20 12:34）

- 第二轮切换成功；运行时环境摘要及 PG/QStash/Nginx 依赖容器一致。应用重启次数为 0。
- 首轮 12:30 因新增扫描计划触发严格校验，12:31 自动恢复旧应用；修正发布校验后重新停写备份并切换。两次均未恢复数据库覆盖业务数据。
- 最终停写备份 `/opt/lobechat-releases/v2217-20260920/cutover-backup-20260920-123223/`，包含数据库、配置及 SHA256，归档可列举；同时复制到开发主机私有目录并验证 SHA256。
- 切换后 478 用户、6347 话题、89169 消息、9517 文件、1378 Agent，与切换前一致；迁移 160 → 162。
- 生产首页（未登录时跳本域名 signin）、登录页、会话接口均返回 200；没有跳转开发域名。默认额度仍 ¥15，生产 OnlyBoxes 未启用。
- 两项任务计划已复核为启用。首轮调查手动暂停过新增计划；发现 QStash 重建定义不自动解除其暂停后，显式恢复并再次验证。不能仅按计划存在就声称调度正常。
- 用户已通过正常流程登录生产。可见 Chrome 验收首页无裂图、全览 6347 话题及费用、全部 / 最近更新筛选、刷新保留全览路由、模型管理界面及 ¥15 限额；截图已实际查看。当前用户会话下六项管理接口全部通过，未修改配置。未注入生产会话、未代用户发送真实模型任务；完整模型 / 附件 / 云端导出验收尚未完成。
