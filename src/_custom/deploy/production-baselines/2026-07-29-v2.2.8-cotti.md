# Production baseline: 2026-07-29 Cotti v2.2.8

This record captures the accepted production baseline before the upstream
LobeHub v2.2.13 upgrade. It contains no credentials.

## Application

- Base package version: `2.2.8`
- Production image:
  `sg-ai-han-registry.ap-southeast-1.cr.aliyuncs.com/lobechat/lobehub:v2.2.8-cotti-prod-20260729-5af09e8448`
- Immutable image digest:
  `sha256:78ea489712f9e262069539cec57dfd817fb7c6caee911271a11dedd3c2c76e01`
- Application source commit: `5af09e84482776f0213fc2da691668e94a815252`
- Repository operations baseline: `694a0c00ecc96c8bb729c10c27a001d946c387d9`
- Previous application image:
  `sg-ai-han-registry.ap-southeast-1.cr.aliyuncs.com/lobechat/lobehub:v2.2.8-cotti-prod-20260729-90cb874da2`
- Previous image digest:
  `sha256:69a7ee52a5f8b24e234b5f52df3d3778a239736b8622966bca37d16da066fec4`

## Runtime topology

The accepted Compose deployment contains all four required services:

- `app`: running and healthy
- `postgresql`: running and healthy
- `qstash`: running; authenticated `/v2/topics` returned HTTP 200
- `searxng`: running

The application uses queued Agent runtime mode with local QStash. Cloudflare
WARP exclusions are configured for the registry and release SSH endpoints.

## Database and rollback

- Validated pre-update PostgreSQL dump:
  `backups/lobechat-pre-app-update-20260729163525.dump`
- Pre-update environment backup: `backups/.env.20260729163525`
- Pre-platform-management environment backup:
  `backups/.env.before-platform-management-20260729163325`
- Latest fork migration at acceptance:
  `0118_cotti_gemini_model_upgrade` (`created_at=1784687043746`)
- Required Cotti platform, audit, home-notification, and model-display tables
  were present.

The one stale virtual Agent row restored by an old client was repaired under a
guarded transaction. Final persisted model counts were:

- `vertexai/gemini-3.5-flash-lite`: 26 Agents
- `vertexai/gemini-3.6-flash`: 93 Agents
- Old Cotti Gemini model IDs: 0 Agents

## Acceptance result

The final production verification completed with `Acceptance checks passed`:

- image tag and immutable digest matched;
- all required services were present and healthy where applicable;
- database migration and Next.js startup completed;
- platform management was compiled into the SPA;
- the runtime platform-management flag and explicit two-account administrator
  allowlist were set;
- provider credentials were present without being printed;
- QStash authentication succeeded;
- Cotti model display mappings matched the intended quick/professional models.

Known non-blocking follow-ups at this baseline:

- Composio is unavailable until its server API key is configured or the
  integration is hidden.
- Existing stale Community OIDC sessions may need Community sign-out and
  re-authorization.
- QStash credentials printed by an older verification script should be rotated
  during a controlled maintenance window.
