# 2026-09-23 生产发布记录（已完成）

目标：`chat.cotticoffee.com`，10.82.70.138，应用容器 `lobechat-app`。

- 发布前：`lobehub:lingshu-v2217-multipart-prod-20260923-r2`。
- 已发布：`lobehub:lingshu-v2217-onlyboxes-export-dev-20260923-r1`，源码 `bf2e92815d`。
- 候选摘要：`sha256:4fc2515f2d3b2d934536e16a8eee604fcfe021bdc14a852ed5f9758e3b992da5`。
- 生产私有准备目录：`/opt/lobechat-releases/v2217-upload-sandbox-20260923`。

## 范围与隔离

候选包含开发侧的全览模式 / 只读活动跟踪、沙箱授权预检、上传阶段进度、大 CSV 普通对话保护和 OnlyBoxes 相对路径导出。生产现有分片上传修复继续保留。数据库迁移文件与生产镜像逐字节一致，162 条迁移，不引入表结构变更或历史数据回填。

生产环境变量逐项保留：模型、认证、OSS、数据库、QStash 均不使用开发配置覆盖。生产已于后续独立配置操作启用 OnlyBoxes，候选发布包已刷新快照，正式发布须保留其配置；详见 ../onlyboxes-production/README.md。保留应用现有 6 GiB 内存限制；不清理生产其他服务。

## 验证与风险

- OnlyBoxes provider 23 项、附加功能 60 项、上传进度 45 项回归通过；类型检查通过。
- 开发 Chrome 验收自建沙箱执行 / 读写 / 下载通过；相对导出首次成功、实际下载内容一致。
- UI 库 5.44.0 → 5.48.2 随完整构建进入候选，扩大了前端回归范围。
- 开发 Chrome 实际上传 82.5 MiB 合成 CSV，观察到 90% / 74.3/82.5 MB 后完成，移除测试附件且未发送模型。校验和合并的短暂阶段未完整动态捕获，仍以自动化用例为证据。
- 发布脚本默认只读；获准后执行 --execute。切换前检查进行中任务，并获取停机一致性备份；没有回灌准备期备份。
- 应用回退保留发布后数据库数据，不自动回灌旧归档。

## 交付

生产目录包含候选镜像包、候选 / 回退 Compose（私有）、固定镜像及配置哈希 manifest、只读预检 / 切换脚本、验证记录、在线准备备份及断网恢复演练脚本。配置和数据库备份不提交 Git。

下一步：观察生产真实上传、OnlyBoxes 并发及错误率；Docker 升级另设维护窗口，不与应用发布合并。

## 准备结果

生产只读预检通过，配置 / 依赖 / 镜像 / 调度定义保持一致。在线数据库备份已在无网络、无对外端口的临时 ParadeDB 完整恢复，162 条迁移校验通过；临时数据库容器及其卷已删除。首次演练的角色初始化、初始化阶段连接中断及预置 schema 冲突均在隔离环境修正；没有修改生产数据库。

## 正式切换与验收

2026-09-23 18:32（北京时间）完成应用切换。最终镜像摘要与开发候选一致；环境变量、PostgreSQL / QStash / Nginx 容器及调度定义保留，162 条迁移未变。OnlyBoxes 独立网络及默认平台容量 2 保留。Docker 27.5.1 未升级。

停机备份位于生产私有目录 `/opt/lobechat-releases/v2217-upload-sandbox-20260923/cutover-backup-20260923-183013`，包含约 385 MiB 数据库归档、SHA256 与配置归档。发布结果写入该发布包 `release-result.json`。应用回退命令为该包 `release.py --rollback --execute`，只回退应用，不恢复旧数据。

预检曾等待用户回复。发现一条重试残留空消息后，只读核实同父消息 / 同用户 / 同话题的替代回复已完成并有用量；发布脚本仅对该明确记录增加替代回复断言并排除，其他未完成回复和运行中任务继续阻止发布，没有修改或删除用户数据。

生产 Chrome 已登录管理员：新建 `tpc_hwJZWe6xoOmK`，实际 metadata.sandboxProvider=onlyboxes；executeCode 执行 Python 计算 / 写入 / 读取成功，exportFile 使用相对路径 prod\_check.txt 首次成功。下载 `/f/file_ul1ueOZOcR5p` HTTP 200，11 字节内容 `7 * 8 = 56\n`，并实际点击下载。2 次工具调用、3 步，话题预估 ¥0.1609。保留该验收话题供复核。

发布后公网会话接口 200，现有管理员登录保持有效；全览可加载并显示模式信息。应用 healthy /restart=0，QStash running /paused=false，OnlyBoxes Console / Worker active。本轮没有重复完整云端社区 OAuth 授权流程。
