# COTTI 服务端能力按业务域移植清单（v2.2.13）

## 目标与约束

- 基线：官方 `v2.2.13`。
- 来源：生产分支 `release/db-migration-home-notification-current` 相对 `v2.2.8` 的二开能力。
- 目标：保留仍有业务价值的 COTTI 能力，不把旧版本实现方式原样搬进新架构。
- 约束：优先新增 COTTI 文件；只有路由注册、鉴权执行和数据采集等无法旁路的位置才修改上游文件。
- 每个业务域单独提交、单独测试、可单独回退。禁止一次性 cherry-pick 跨域旧提交。

## 统一落位规则

新增服务端代码统一落在以下位置：

```text
apps/server/src/routers/lambda/cotti/   # COTTI tRPC 路由
apps/server/src/services/cotti/         # COTTI 服务与策略
src/_custom/                            # COTTI 前端、注册表、升级文档
src/types/cotti/                        # 需要前后端共享的 COTTI API 类型
packages/database/src/models/cotti*     # COTTI 数据模型（已经存在）
packages/database/src/schemas/cotti*    # COTTI 表结构（已经存在）
```

根路由只注册一个 `cotti` 子路由：

```text
lambdaClient.cotti.admin
lambdaClient.cotti.agentAccess
lambdaClient.cotti.homeNotification
lambdaClient.cotti.modelDisplay
lambdaClient.cotti.platformAnalytics
lambdaClient.cotti.platformAudit
```

这样后续增加 COTTI API 时，不再反复修改上游根路由。

## 业务域取舍结论

| 业务域                                    | 决策                            | 原因                                                                                                                         | 预计上游触点                                         |
| ----------------------------------------- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| 数据库表与历史数据                        | 已完成                          | 0112–0119 已完成重排、预处理和生产数据兼容演练                                                                               | 已完成                                               |
| COTTI 平台管理员鉴权                      | 现在做                          | 是所有平台管理接口的共同前置能力                                                                                             | 根路由 1 处                                          |
| 首页通知管理                              | 现在做                          | 生产已使用；模型和表已存在；低耦合                                                                                           | 无新增上游触点                                       |
| 平台模型展示配置                          | 现在做，但按新架构重写          | 生产已使用；v2.2.13 有用户级模型显示名，但没有平台统一的 Chat/Agent 模型池                                                   | 模型访问策略 1–2 处                                  |
| Agent 功能准入                            | 现在做，但收敛执行点            | 生产已有 open/off/allowlist 管理；需要服务端防绕过                                                                           | Agent 创建、更新、执行入口约 3–5 处                  |
| 平台数据分析与反馈                        | 现在做，拆成两次提交            | 生产平台管理依赖；旧服务约 720 行 SQL，需要适配新 schema                                                                     | Market 反馈落库 1 处                                 |
| 平台内容审计                              | 后续做，先迁 API 再启用 AI 分析 | 生产已有能力，但旧服务约 849 行，涉及隐私、模型调用成本和新消息结构                                                          | 原则上无上游触点                                     |
| 飞书蓝标第二 SSO                          | 现在做                          | 生产登录链路依赖；改动小、边界清晰                                                                                           | Auth env、Feishu provider、provider registry 共 3 处 |
| 社区登录拒绝后的换号、登出                | 服务端阶段不重复改；列入前端域  | v2.2.13 已有 OIDC/Market 服务端路由和本地 token 登出基础；生产修复主要在客户端状态与回调错误恢复                             | 服务端 0 处                                          |
| Excel 解析                                | 不移植旧实现                    | v2.2.13 已在 `packages/file-loaders` 原生支持 Excel，旧 loader 已被新包替代                                                  | 0 处                                                 |
| 定时任务隔离策略                          | 暂缓，需业务确认                | v2.2.13 已有隔离线程和新版 Task/Agent Runtime；旧策略还额外禁止文档、知识库、历史等能力，属于产品 / 安全策略而非普通兼容补丁 | 若保留将涉及多个运行时触点                           |
| Agent / 消息附件 payload 裁剪             | 暂缓，先测接口体积              | v2.2.13 引入新版 Agent Documents 和渐进式内容接口；旧投影补丁不可直接套用                                                    | 复现后再定                                           |
| 流式 tool-call 稳定补丁                   | 默认不移植                      | Model Runtime 和流协议已大幅变化；只在目标模型回归失败时做新修复                                                             | 复现后再定                                           |
| Azure/Qwen/Volcengine 大范围 Runtime 补丁 | 不整体移植                      | 生产所需 Gemini、GPT-5.5/5.6、GLM、Qwen、豆包 Chat 模型大多已被 v2.2.13 原生收录                                             | 0 处或单模型卡                                       |
| Seedream 5.0 Pro                          | 单独验证后决定                  | v2.2.13 有 Seedream 5.0 Lite，没有生产使用的 Pro ID                                                                          | 必要时只加模型卡和针对性适配                         |
| 全局 Provider / 模型裁剪                  | 不搬旧核心补丁                  | 优先使用环境变量、平台模型配置和现有模型访问策略                                                                             | 0 处                                                 |

## 实施阶段与小步骤

### S1：COTTI 服务端骨架与平台管理员鉴权

1. 新建 `routers/lambda/cotti/index.ts`，只暴露 COTTI 子路由。
2. 在根 `lambdaRouter` 注册一次 `cotti: cottiRouter`。
3. 新建 `services/cotti/adminAccess.ts`。
4. 管理员判定兼容生产环境变量 `COTTI_PLATFORM_ANALYTICS_ADMIN_EMAILS`。
5. 同时识别 v2.2.13 全局 RBAC 角色 `super_admin`，并兼容生产既有的 Better Auth `users.role=admin`；不自动创建或修改任何角色数据。
6. 新增 `cotti.admin.getAccess`，供平台管理入口做服务端权限确认。
7. 单测覆盖：未登录、普通用户、邮件白名单、规范化邮件、`super_admin`、通配符配置。

验收：普通用户得到 `FORBIDDEN`；两类管理员可访问；不改变现有普通 LobeHub API。

### S2：低耦合平台配置

#### S2.1 首页通知

1. 移植公开读取和管理员更新两个 API 到 `cotti.homeNotification`。
2. 复用现有 `CottiHomeNotificationModel`，不改 schema。
3. 输入 schema 放在 COTTI 路由内，共享返回类型放入 COTTI 类型目录。
4. 增加路由权限测试、空配置默认值测试和更新测试。

验收：未登录用户只能读取；管理员可更新；普通登录用户不能更新。

#### S2.2 平台模型展示配置

1. 移植配置读取、模型选项读取、管理员更新 API。
2. 复用 `CottiModelDisplayModel`，保留 Chat 与 Agent 两套列表。
3. 将 “平台可见模型” 接入 v2.2.13 的 `aiProviderAccess` 策略层，不向 `AiInfraRepos` 重新塞旧版构造参数。
4. 明确策略合并顺序：上游 / 用户禁用优先，COTTI 平台配置只能进一步收窄，不能重新放开被上游禁止的模型。
5. 保留 v2.2.13 用户模型显示名能力；COTTI 配置只覆盖平台指定模型的展示名。
6. 增加 Chat/Agent 场景过滤、分页后过滤、运行时状态过滤和显示名测试。

验收：平台关闭的模型不出现在对应场景；用户和上游的禁用规则不被绕过；现有模型运行时不受无关配置影响。

### S3：飞书蓝标第二 SSO

1. 增加 `AUTH_FEISHU_BLUE_APP_ID` 和 `AUTH_FEISHU_BLUE_APP_SECRET` 环境声明与校验。
2. 将现有 Feishu provider 提取为同文件工厂，保持默认 `feishu` 行为不变。
3. 注册 `feishu-blue` provider，使用独立 provider ID 和回调地址。
4. 无真实密钥单测 provider 注册、缺失变量报错、两个 provider ID 与 fallback email 域隔离。
5. 在 dev 使用测试应用完成一次登录回调验收。

验收：原飞书登录不回归；蓝标账号生成独立 account/provider 绑定；不会串号。

### S4：Agent 功能准入

1. 移植纯策略函数：open/off/allowlist、身份规范化、强制 Chat-only。
2. 移植 DB 规则解析服务，复用 `CottiAgentAccessModel`。
3. 新增管理员配置与用户搜索 API 到 `cotti.agentAccess`。
4. 在 Agent 创建、更新和实际执行入口统一调用准入服务；读取接口只做 UI 状态归一化，不能作为唯一防线。
5. 禁止无权限用户通过直接 API 把 `enableAgentMode` 改回 true。
6. 对同一请求中的重复判定做请求级复用，避免每次读取 Agent 都重复查库。
7. 单测 open/off/allowlist、环境配置 fallback、DB 规则优先级、API 绕过和执行入口。

验收：即使篡改客户端请求，无权限用户也不能执行 Agent 模式；Chat 模式不受影响。

### S5：平台数据分析与反馈

#### S5.1 反馈本地留存

1. 把 Market `submitFeedback` 成功后的本地落库封装成 `services/cotti/feedback`。
2. Market 原路由只增加一次非阻断调用；本地落库失败不能影响提交到 LobeHub Market。
3. 日志不输出 token、邮件正文或截图内容。
4. 增加 Market 成功 / 失败、本地 DB 成功 / 失败测试。

#### S5.2 数据看板

1. 按 “总览、趋势、用户、模型、功能、反馈” 拆分旧 720 行 SQL，不保留单文件大服务。
2. 每条查询逐项对照 v2.2.13 schema，特别检查 workspace、generation、agent operation 和消息字段。
3. 保留 Asia/Shanghai 统计口径，并在类型和测试里固定边界时间。
4. 所有路由使用统一平台管理员 guard。
5. 使用隔离 PostgreSQL 写小数据集集成测试，验证计数、去重和分页。

验收：与同一份固定数据集的手工 SQL 结果一致；查询有明确时间范围和分页上限；普通用户不可访问。

### S6：平台内容审计

1. 先迁只读消息列表、详情和查看日志，不立即打开 AI 风险分析。
2. 将旧 849 行服务拆为查询、风险规则、模型分析和查看日志四个模块。
3. 对 v2.2.13 消息、文件、workspace 可见性做重新授权设计，不能只依赖 “是平台管理员”。
4. 明确审计日志中的管理员、目标消息、时间和元数据最小集合。
5. AI 分析单独受环境开关控制，设置超时、模型白名单和失败降级。
6. 风险分析写入使用幂等键，避免重复点击产生并发调用。
7. 用脱敏测试数据验证查询和日志；真实内容不得出现在测试快照。

验收：只读审计稳定后再启用 AI；AI 不可用时不影响详情查看；每次敏感内容查看均留痕。

## 暂缓域的重新启用条件

### 定时任务隔离

必须先确认以下业务问题：

- 定时任务是否允许读取当前 Agent 的文档和知识库？
- 是否允许读取话题历史？
- 是否允许执行 `lh agent/doc/file/kb/message/topic`？
- “隔离” 是防止数据越权，还是仅避免污染当前会话？

只有答案明确后，先为 v2.2.13 写失败测试，再在新 Runtime 的单一策略边界实现。旧 `taskIsolationPolicy.ts` 不直接移植。

### Payload 裁剪

先记录以下基线：Agent 配置响应大小、消息列表响应大小、带 Excel/PDF 附件时首轮聊天请求大小、数据库查询耗时。只有超过约定阈值或出现传输失败，才针对当前查询投影优化。

### 流式工具调用与模型 Runtime

针对生产模型执行文本、思考、视觉、搜索、函数调用、图片生成的冒烟测试。旧 Runtime 补丁只作为问题线索，不作为移植来源。

## 每个提交的强制检查

1. `git diff --name-only` 标注新增 COTTI 文件与上游触点。
2. 每个上游触点在提交说明中写明 “为何无法旁路”。
3. 只运行相关 Vitest；完成一个阶段后运行 `bun run type-check`。
4. 涉及 SQL 的阶段必须跑隔离 PostgreSQL 集成测试。
5. 不混入 UI、部署和模型配置之外的顺手重构。
6. 每个业务域完成后再提交，不把多个域压成一个不可回退的提交。

## 建议执行顺序

```text
S1 管理员鉴权
  -> S2.1 首页通知
  -> S2.2 模型展示配置
  -> S3 飞书蓝标 SSO
  -> S4 Agent 准入
  -> S5.1 反馈留存
  -> S5.2 数据看板
  -> S6 内容审计
  -> 暂缓域按回归结果决定
```

首个代码实施域应为 **S1：COTTI 服务端骨架与平台管理员鉴权**。它对上游只产生一次根路由注册，后续平台能力都可在 COTTI 命名空间内追加。
