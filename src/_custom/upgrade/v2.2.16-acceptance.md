# 当前验收与剩余任务

更新日期：2026-09-09。已有测试和真实样本证据保存在私有.records中；不把旧测试数量相加当作本轮重新通过。

## 已完成

- 50项源码二开逐项静态核对，包含实际消费者；未发现明确漏合并。
- 两个应用镜像SHA一致，共用PG/Redis/QStash；默认网关优先级已固定，两个容器推荐接口200并返回有效数据。
- 认证网络OIDC发现文档200，两个域名登录入口保持各自地址；这不是真人OAuth完整回调验收。
- 152×152人像及提示框定位补丁、推荐错误重试提示均双入口发布；相关13/17项测试和布局/类型检查已有证据。
- 历史增量已正式合并，原生读取与权限校验完成；图片下载有真实落盘样本验证。
- 本次文档已全量备份并去除旧版本、废弃方案和重复过程记录。

## 本轮登录态补验

Chrome CDP已恢复。两个域名均进入首页，推荐卡片与人像真实渲染；chatdev显示未读任务与最近话题。Portal登录前后保持chat.cotti.ai，chatdev保持自身域名。

两入口分别在1826、1280、1000、800 CSS像素宽度检查：人像框始终152×152，提示框约251.59像素宽，与图片框间隔约8像素，没有横向溢出；宽屏和800像素截图已目视核对。8像素为图片框间隔，图片透明区域仍会增加可见人物距离。测试后已清除视口模拟。几何证据：`.records/native-v2216/logged-home-final/layout.json`。

账号已按用户授权完成关联：保留原飞书用户，把generic-oidc登录绑定指向同一用户，删除Portal侧空白用户及其1个Agent。原15个Agent、881个话题、7684条消息数量不变；主用户在两个容器均命中既有管理员白名单。旧账号DB会话已删除，实际redis-history中的旧会话缓存已定向清理。Chrome中Portal重新认证成功，chat.cotti.ai会话返回原飞书用户ID，首页显示原姓名、任务、最近话题及全览入口；两个域名保持独立。本次只修改数据，无代码发布，不新增源码二开编号。完整数据库和受影响行备份：`.records/native-v2216/account-link-20260909/`。

几何探针中的recommendation字段仅匹配按钮textContent，不用于推荐渲染结论；实际截图与无障碍树均显示推荐卡片。

## 邮箱验证码验收完成

Chrome实际发送验证码后进入6位输入页面；用户操作后同一页面进入首页，会话接口200、已认证且邮箱与授权测试邮箱一致。验证码登录端到端通过。没有点击邮件链接或读取数据库验证码，没有合并测试邮箱账号。

## 用户后置验收


蓝版飞书真人登录由用户明确后置自行测试。

邮件配置已在两个应用容器生效：复用原SMTP，SMTP_FROM显示名为灵枢AI，AUTH_ENABLE_EMAIL_OTP=1、AUTH_ENABLE_MAGIC_LINK=0、AUTH_DISABLE_EMAIL_PASSWORD=0、AUTH_EMAIL_VERIFICATION=1。原生邮件正文/主题仍为LobeHub模板。DEV-050以同一新镜像更新两个应用。验证码复用原生6位/5分钟/3次尝试限制，并增加60秒重发冷却及并发邮箱锁。46项初次相关测试通过，并发修复后36项登录测试通过，全量类型检查通过，独立复核通过。备份与证据：`.records/native-v2216/email-login-20260909/`。

## 后续运维边界

- 历史OSS：缺失原件和哈希不一致不能用直接改size/hash掩盖；需原件/数据确认。
- Azure实账单归属与平台成本记录需分别核查；历史模型标签不代表实际调用。
- 已明确后置的搜索质量、统一SSO总体方案、云数据库和队列持久化不列为本次源码漏合并。

## 下一步

账号关联和邮箱验证码登录已完成；蓝版飞书由用户后续测试。首页推荐和响应式定位本轮已验收，不重复列为待办。

## DEV-050 登录页原位验证码

按用户最终选择复用原signin邮箱输入框：先输入邮箱并获取验证码，再在相同表单位置输入验证码；不切换独立验证码页面，飞书入口、标题和协议保留。主按钮按阶段显示获取验证码/验证并登录，发送后小字标明收件邮箱及有效期，提供重发/更换邮箱。更换邮箱清空验证码和错误并恢复原邮箱。36项相关测试、lint和全量类型检查通过，独立复核无阻断问题；两个入口同镜像发布，仍计DEV-050。

Chrome本轮实际验证：发码后URL和登录表单保留，页面只有1个文本输入框；输入3位数字按Enter显示长度错误；换邮箱恢复原邮箱并清除验证码错误；冷却期内返回验证码不重复发信。桌面与390px窄屏目视检查完成。登录成功业务沿用前轮已通过的原生OTP流程，本轮没有再次输入真实验证码登录。

## DEV-051 上下文成本保护与新话题入口（已发布验收）

- 里程碑后新增能力，原有50项源码基线保持独立Tag。
- 依据目录中的输入涨价分档计算成本阈值；未知价格使用平台预算，不宣称是实际供应商分档。Terra初次/再次压缩上限190,400，最终请求预算231,200，均使用原生Token估算安全系数1.25校正后的计数，因此通常更早触发。
- 模型调用前重新计算完整messages与tools，包含缓存读取对应的上下文；普通Chat和结构化输出均检查。超过预算抛出可读错误、停止本次请求。尚未实现持久化“等待管理员放行”任务状态。
- 新问题创建空话题并保存当前草稿；带着进展继续按完整话题记录生成分段滚动摘要，每段最多32,000字符，摘要响应上限4096输出Token；保留目标、重要数字、下一步与原话题链接。原话题不改写、不删除。
- 当前续聊使用新话题到原话题的单向链接；未新增旧话题回链。Agent自身配置和知识库仍会按原生规则应用，新话题并不等于没有Agent级上下文。
- 已通过首轮318项相关测试，以及入口/参数修正18项、草稿恢复14项等定向检查（各轮有重复），lint和全量类型检查通过。最终生产构建成功。
- 本批边界：不包含语义识别分流、一次性高价放行、Job自动按日创建话题、工具结果去重/按需检索策略、平台管理的新增分项审计。

### DEV-051 实机结果

- 两个应用统一镜像 `lobehub:lingshu-v2216-topic-cost-20260910-r4`，SHA `6a94db125414d83e2e1e67025e15c6b0a030080190978eeee84103a075126c01`；会话接口均200，app-cotti默认网关仍为应用网络192.168.48.1。QStash启动时间未变。
- 两域名的真实认证请求均将合成72万Token输入拒绝为HTTP400 / CONTEXT_COST_LIMIT；未发送上游。小规模Terra正常对话通过。
- Chrome完整验收：摘要保留“晨光、123元、完成报告”和原话题链接；当前草稿迁移成功；新问题创建时数据库消息数为0；恢复的草稿可直接点击发送，返回“通过”。
- 离线创建失败显示重试提示，原URL和草稿保留；慢网络下取消续聊返回空闲，未追加摘要模型请求。
- 桌面、收起原生侧栏后的390px窄屏以及手机UA的390px视图完成目视检查，两种入口可见。桌面侧栏展开时的整体极窄布局沿用原生限制，未宣称该场景通过。
- 证据保存在 `.records/native-v2216/topic-cost-20260910/`。手机UA检查触发Chrome MCP离页确认等待；手机截图通过同一9222的直接CDP完成，已恢复桌面UA与视口、保留浏览器登录态。本次卡住的MCP进程已结束，工具传输需新会话重连；应用服务不受影响。
- 本批保护的是平台服务端ModelRuntime调用与内置Agent运行路径；外部CLI或用户绕过服务端直接调用供应商不在本次覆盖范围。计数为带安全余量的估算，不是供应商逐Token计费保证。
