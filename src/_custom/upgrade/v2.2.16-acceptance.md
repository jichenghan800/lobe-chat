# 当前验收与剩余任务

更新日期：2026-09-09。已有测试和真实样本证据保存在私有.records 中；不把旧测试数量相加当作本轮重新通过。

## 已完成

- 50 项源码二开逐项静态核对，包含实际消费者；未发现明确漏合并。
- 两个应用镜像 SHA 一致，共用 PG/Redis/QStash；默认网关优先级已固定，两个容器推荐接口 200 并返回有效数据。
- 认证网络 OIDC 发现文档 200，两个域名登录入口保持各自地址；这不是真人 OAuth 完整回调验收。
- 152×152 人像及提示框定位补丁、推荐错误重试提示均双入口发布；相关 13/17 项测试和布局 / 类型检查已有证据。
- 历史增量已正式合并，原生读取与权限校验完成；图片下载有真实落盘样本验证。
- 本次文档已全量备份并去除旧版本、废弃方案和重复过程记录。

## 本轮登录态补验

Chrome CDP 已恢复。两个域名均进入首页，推荐卡片与人像真实渲染；chatdev 显示未读任务与最近话题。Portal 登录前后保持 chat.cotti.ai，chatdev 保持自身域名。

两入口分别在 1826、1280、1000、800 CSS 像素宽度检查：人像框始终 152×152，提示框约 251.59 像素宽，与图片框间隔约 8 像素，没有横向溢出；宽屏和 800 像素截图已目视核对。8 像素为图片框间隔，图片透明区域仍会增加可见人物距离。测试后已清除视口模拟。几何证据：`.records/native-v2216/logged-home-final/layout.json`。

账号已按用户授权完成关联：保留原飞书用户，把 generic-oidc 登录绑定指向同一用户，删除 Portal 侧空白用户及其 1 个 Agent。原 15 个 Agent、881 个话题、7684 条消息数量不变；主用户在两个容器均命中既有管理员白名单。旧账号 DB 会话已删除，实际 redis-history 中的旧会话缓存已定向清理。Chrome 中 Portal 重新认证成功，chat.cotti.ai 会话返回原飞书用户 ID，首页显示原姓名、任务、最近话题及全览入口；两个域名保持独立。本次只修改数据，无代码发布，不新增源码二开编号。完整数据库和受影响行备份：`.records/native-v2216/account-link-20260909/`。

几何探针中的 recommendation 字段仅匹配按钮 textContent，不用于推荐渲染结论；实际截图与无障碍树均显示推荐卡片。

## 邮箱验证码验收完成

Chrome 实际发送验证码后进入 6 位输入页面；用户操作后同一页面进入首页，会话接口 200、已认证且邮箱与授权测试邮箱一致。验证码登录端到端通过。没有点击邮件链接或读取数据库验证码，没有合并测试邮箱账号。

## 用户后置验收

蓝版飞书真人登录由用户明确后置自行测试。

邮件配置已在两个应用容器生效：复用原 SMTP，SMTP\_FROM 显示名为灵枢 AI，AUTH\_ENABLE\_EMAIL\_OTP=1、AUTH\_ENABLE\_MAGIC\_LINK=0、AUTH\_DISABLE\_EMAIL\_PASSWORD=0、AUTH\_EMAIL\_VERIFICATION=1。原生邮件正文 / 主题仍为 LobeHub 模板。DEV-050 以同一新镜像更新两个应用。验证码复用原生 6 位 / 5 分钟 / 3 次尝试限制，并增加 60 秒重发冷却及并发邮箱锁。46 项初次相关测试通过，并发修复后 36 项登录测试通过，全量类型检查通过，独立复核通过。备份与证据：`.records/native-v2216/email-login-20260909/`。

## 后续运维边界

- 历史 OSS：缺失原件和哈希不一致不能用直接改 size/hash 掩盖；需原件 / 数据确认。
- Azure 实账单归属与平台成本记录需分别核查；历史模型标签不代表实际调用。
- 已明确后置的搜索质量、统一 SSO 总体方案、云数据库和队列持久化不列为本次源码漏合并。

## 下一步

账号关联和邮箱验证码登录已完成；蓝版飞书由用户后续测试。首页推荐和响应式定位本轮已验收，不重复列为待办。

## DEV-050 登录页原位验证码

按用户最终选择复用原 signin 邮箱输入框：先输入邮箱并获取验证码，再在相同表单位置输入验证码；不切换独立验证码页面，飞书入口、标题和协议保留。主按钮按阶段显示获取验证码 / 验证并登录，发送后小字标明收件邮箱及有效期，提供重发 / 更换邮箱。更换邮箱清空验证码和错误并恢复原邮箱。36 项相关测试、lint 和全量类型检查通过，独立复核无阻断问题；两个入口同镜像发布，仍计 DEV-050。

Chrome 本轮实际验证：发码后 URL 和登录表单保留，页面只有 1 个文本输入框；输入 3 位数字按 Enter 显示长度错误；换邮箱恢复原邮箱并清除验证码错误；冷却期内返回验证码不重复发信。桌面与 390px 窄屏目视检查完成。登录成功业务沿用前轮已通过的原生 OTP 流程，本轮没有再次输入真实验证码登录。

## DEV-051 上下文成本保护与新话题入口（已发布验收）

- 里程碑后新增能力，原有 50 项源码基线保持独立 Tag。
- 依据目录中的输入涨价分档计算成本阈值；未知价格使用平台预算，不宣称是实际供应商分档。运行目录目前为 Terra 返回 272,000 容量，未返回 pricing，采用平台预算；实际压缩 / 冻结阈值 136,000，最终请求预算 231,200，均比较原生 Token 估算安全系数 1.25 校正后的计数。
- 模型调用前重新计算完整 messages 与 tools，包含缓存读取对应的上下文；普通 Chat 和结构化输出均检查。超过预算抛出可读错误、停止本次请求。尚未实现持久化 “等待管理员放行” 任务状态。
- 新问题创建空话题并保存当前草稿；带着进展继续按完整话题记录生成分段滚动摘要，每段最多 32,000 字符，摘要响应上限 4096 输出 Token；保留目标、重要数字、下一步与原话题链接。原话题不改写、不删除。
- 当前续聊使用新话题到原话题的单向链接；未新增旧话题回链。Agent 自身配置和知识库仍会按原生规则应用，新话题并不等于没有 Agent 级上下文。
- 已通过首轮 318 项相关测试，以及入口 / 参数修正 18 项、草稿恢复 14 项等定向检查（各轮有重复），lint 和全量类型检查通过。最终生产构建成功。
- 本批边界：不包含任意问题的语义相似度分类、一次性高价放行、Job 自动按日创建话题、工具结果去重 / 按需检索策略、平台管理的新增分项审计。

### DEV-051 实机结果

- 两个应用统一镜像 `lobehub:lingshu-v2216-topic-dialog-20260910-r4`，SHA `5916fb7e63bcc19f69d91e4442d0ccc4a7052df48d99386a2dd0876ec06fa476`；会话接口均 200，app-cotti 默认网关仍为应用网络 192.168.48.1。QStash 启动时间未变。
- 两域名的真实认证请求均将合成 72 万 Token 输入拒绝为 HTTP400 / CONTEXT\_COST\_LIMIT；未发送上游。小规模 Terra 正常对话通过。
- Chrome 完整验收：摘要保留 “晨光、123 元、完成报告” 和原话题链接；当前草稿迁移成功；新问题创建时数据库消息数为 0；恢复的草稿可直接点击发送，返回 “通过”。
- 离线创建失败显示重试提示，原 URL 和草稿保留；慢网络下取消续聊返回空闲，未追加摘要模型请求。
- 桌面、收起原生侧栏后的 390px 窄屏以及手机 UA 的 390px 视图完成目视检查，两种入口可见。桌面侧栏展开时的整体极窄布局沿用原生限制，未宣称该场景通过。
- 证据保存在 `.records/native-v2216/topic-cost-20260910/`。手机 UA 检查触发 Chrome MCP 离页确认等待；手机截图通过同一 9222 的直接 CDP 完成，已恢复桌面 UA 与视口、保留浏览器登录态。本次卡住的 MCP 进程已结束，工具传输需新会话重连；应用服务不受影响。
- 本批保护的是平台服务端 ModelRuntime 调用与内置 Agent 运行路径；外部 CLI 或用户绕过服务端直接调用供应商不在本次覆盖范围。计数为带安全余量的估算，不是供应商逐 Token 计费保证。

## DEV-051 补充与 DEV-052 强制冻结（本轮验收）

- 新问题轻提示只检测明确换题开场语，例如 “换个话题”“一个不相关的问题”。在当前输入区建议用户使用新问题，支持暂不提醒，当前组件会话内按话题记住关闭；不拦截普通发送、不做任意语义相似度分类、不新增识别模型调用。

- 修正开发者模式下两个入口缺失；底栏由固定 28px 改为最小 28px、按内容增高和换行，避免说明与按钮溢出。

- `topic_cost_freezes` 是服务端独立持久表，话题普通修改、摘要压缩和换模型不能清除冻结；删除话题时由外键级联清理。没有自动解冻、手动高价放行或冻结任务自动迁移。

- 独立冻结阈值（不再传入原生压缩）：min (目录容量 ×50%, 成本预算 ×70%)。当前 Terra 实际为校正后 136,000（原始估算约 108,800）；超过或达到即在上游调用前落库并拒绝。冻结后即使只发 “你好” 仍拒绝。缓存命中对应的输入也计入，不因为缓存较便宜而允许跨过阈值。

- 普通 Chat 请求的话题 Header/Trace、客户端辅助压缩、服务端 Agent/Job 的调用 metadata 均携带 topicId，统一经过 ModelRuntime 冻结守卫。无话题请求仍受单次输入预算约束；外部 CLI 直连不受本守卫控制。已发出的请求无法追回；这是上下文保护，不是每日费用限额，也不宣称停止已在执行的外部工具。

- 输入区读取冻结状态后禁用发送按钮和快捷键；历史和草稿保留。冻结后续接改走服务端专用摘要接口，验证话题归属及消息创建权限，片段≤32,000 字符、前序摘要≤24,000 字符、输出≤4096 Token、无工具。该受限操作可从冻结话题转到新话题，单次预算守卫仍执行，取消信号传到模型请求。

- 新表在实际数据库事务内测得创建约 34ms、外键约 11ms，已回滚并确认清理。发布由首个应用自动执行 0162 迁移，健康后再更新另一个应用；没有手工重复迁移或重启 QStash。

- 165 项相关测试通过，随后受限摘要 / 守卫 7 项和客户端压缩标识 8 项定向检查通过；轮次有重叠，不累加为独立测试数。完整类型检查和生产构建通过。开发者入口回归在旧代码失败、修复后通过；数据库集成测试验证跨实例读取、所有权隔离、更新不能解冻及级联清理。

- Chrome MCP 已验证冻结真实 HTTP400/TOPIC\_COST\_FROZEN、同一冻结话题短请求和换模型仍 HTTP400、发送禁用、关闭轻提示保留草稿，以及一次真实受限摘要转入新话题，保留项目 / 数字 / 下一步和草稿。新话题无冻结记录。

- 发布后用量观察样本不足以计算业务节省比例，不把无 usage 的空助理占位记录算作零费用。详细证据与最终界面截图保存在`.records/native-v2216/topic-suggestion-20260910/`。

- 最终界面回归：底栏随内容增高，桌面及 390px 窄屏的冻结说明、两个入口均在可视区域内。冻结话题按 Enter 后草稿仍在，数据库消息数未增加；重启双应用后冻结记录及页面状态仍保留。

## DEV-051 换题弹框验收（当前批次）

- 自动命名一次返回标题和不超过 100 字符的描述；命名输入最多 8 条、每条 2000 字符、合计 8000 字符，旧话题不批量补描述。

- 直接用户提交且普通话题空闲时判断换题，输入仅概览≤200 字符和新输入≤1000 字符；沿用命名模型。服务端 4 秒超时，失败或不确定不弹框，模型成本保护独立执行。没有读取最近几轮或整个历史，也没有每轮重新总结描述。

- 弹框主按钮 “新建话题并发送”，次按钮 “当前话题发送”；关闭不发送。消息未发出前检查话题或草稿是否变化，避免错发；新话题保留 Agent、模型、附件及当前消息。

- 普通输入区移除两个常驻按钮；冻结话题保留单个 “转到新话题” 入口。

- 2026-09-10 已撤销全部成本压缩上限覆盖，运行时压缩源码恢复 v2.2.16 官方版本。独立冻结数值暂不变，仍可能先于官方压缩触发；Gemini 3.8 Flash 独立冻结线为校正后 100 万 Token。

- 本批接口和前端相关检查 145 项通过，完整类型检查通过。命名回归先复现 “既有空标题话题响应无 topicId 时不命名” 的失败，回退当前话题标识后，发送生命周期 95 项测试通过；不新增第二次命名请求。

- Chrome 真实模型检查：价格对比话题输入 “查询北京天气” 返回换题，单次请求约 1.3 秒（样本，不承诺固定延迟）；默认焦点在 “新建话题并发送”。Esc 关闭保留草稿；选择新话题只在新话题写入一条用户消息并收到回复；选择当前话题只在原话题写入一条消息。均为独立验收话题，没有改动用户提供的价格分析原话题。

- 现场发现既有 SYSTEM\_AGENT 默认 qwen3.7-plus 已下线，导致自动命名被拒绝。仅 topic 覆盖为平台现有快速默认 vertexai/gemini-3.5-flash-lite；前后端命名 / 换题检查统一读取环境 topic 默认及用户覆盖，其他辅助配置不改。私有环境备份在本批 records 目录。

- 最终真实发送创建 tpc\_0gbPlsAQIaUT，仅一条用户消息；原生命名自动保存标题 Reading Plan 与描述 Create a personalized reading schedule.，遵循当前响应语言配置。没有额外调用独立描述生成。

- 修复发送后双帧回焦与立即弹框的竞争：存在 dialog 时不再抢回编辑器焦点。回归测试在修复前失败；弹框按钮默认动作不再被输入框覆盖。

- 快速模型对照验证：追加千问价格比较返回相关（不弹框），查询北京天气返回换题；约 0.95 秒、1.02 秒。16 项输入区测试、57 项飞书回调测试及 4 项命名配置 / 限长识别测试通过，完整类型检查通过。

- R4 最终 Chrome 验收：桌面与真实 390px CSS 视口均默认聚焦 “新建话题并发送”，按钮没有溢出；关闭后草稿保留，视口已恢复。chat.cotti.ai 登录会话接口返回 200 且仍已登录。两个应用均运行 topic-dialog-20260910-r4 镜像，QStash 未随本次发布重启。

## DEV-009 全用户话题检索验收

- 根因：全览复用合规审计的管理员 / 白名单排除规则。真实话题 “询问助手能力” 所属用户命中登录邮箱白名单，旧查询返回 0，修复后返回 1。

- 列表与详情统一覆盖全部未删除话题，入口仍需平台管理员权限，详情仍记录查看审计。不扩展普通用户权限，不改变合规审计范围。

- PostgreSQL 实测约 1927 个话题：旧首屏查询 588.5ms；修复后首屏 50 条约 67.6ms，标题搜索约 8.0ms。单次 EXPLAIN ANALYZE 样本，非延迟保证。统计仅执行当前页，已有 messages/topic\_id 和 agent\_operations/topic\_id 索引足够；列表不读取对话正文或 OSS 文件，无需新增索引或数据迁移。

- 隔离测试数据库回归：旧版 2 项失败；修复后包含路由鉴权在内 6 项通过，完整类型检查通过。覆盖跨用户检索、白名单 / 管理员、描述检索、分页总数、字面搜索与详情读取。

- 发布后 Chrome 实测：全览从旧版 869 条扩展到当时全部 1928 条（查询期间有新增）。输入 “询问助手能力” 返回 1 条，点击后正常展示 156 条消息、4 张图片和实际模型；数据库确认新增 1 条详情查看审计。截图 search-before.png、search-after.png、detail-after.png 已目视核对，证据保留在私有 `.records/topic-overview-all-20260910/`。

- 两个域名均运行 `lobehub:lingshu-v2216-overview-all-20260910`，会话接口 HTTP200，应用网络默认网关保持 192.168.48.1；PG 与 QStash 启动时间未变化。

## DEV-051 累计 Token 展示

- 复用 topics.total\_tokens/total\_input\_tokens/total\_output\_tokens，原生在消息用量持久化及 Agent 完成时汇总，包含子线程模型调用；不再叠加缓存 / 推理分项。

- 仅最新回答底部显示当前话题累计，回答生成中不请求；按 topicId 和完成消息快照区分缓存，普通模式也显示。无数据 / 读取失败显示暂无统计，不伪装零消耗；文案明确当前上下文与累计消耗的区别。

- 本批 14 项相关测试通过，完整类型检查通过。无数据迁移、无价格公式调整、无新增模型请求。

- Chrome 真实发送复现累计数晚一轮更新：新回复先写入 metadata.usage，刷新后才提升为 usage，仅读 usage 会漏掉最新回复的同步信号；已兼容持久化、嵌套与旧版平铺用量格式，优先级明确且不重复相加。另补充数据库提交时序的下界校验：只合计当前已加载原始助理消息的已知用量作同步检查，不作为最终累计值；服务器汇总未包含这些用量时最多重读 4 次、间隔 500ms，仍未同步则显示暂无统计。没有无限轮询或额外模型调用。

- 最新补充的 9 项用量回归测试与完整类型检查通过，覆盖实时 metadata 格式、缓存不重复计数、缺失与零值、有限重读。

- R3 Chrome 真实回复验收：无需刷新，累计从 13,520 自动更新至 18,083；悬停精确显示输入 18,063、输出 20，与 PG 原生汇总一致。普通对话模式与 390px 视口均可见，截图 loaded-r3.png、after-send-r3.png、tooltip.png、narrow\.png 已目视核对；证据保留在私有 `.records/topic-token-total-20260910/`。

- 两个新版域名均运行 `lobehub:lingshu-v2216-topic-tokens-20260910-r3`，get-session 接口 HTTP200，PG / QStash 启动时间未变化，应用网络默认网关保持 192.168.48.1。首次重载遇到旧静态资源缓存，强制刷新后正常；旧生产 chat.cotticoffee.com 未发布本批功能。

- Chrome 同页路由切换：Reading Plan（18,083）→ 另一验收话题（4,481）→ Reading Plan（18,083），未串用缓存；returned.png 已目视核对，视口与先前验收草稿已恢复。

## DEV-051 / DEV-006 累计模型费用验收

- 统计已保存的每次模型 cost，支持混用 Terra / Gemini，缓存读写与推理输出由原生费用计算负责；重复复制的历史不计入。费用缺失保留为未知并显示统计不完整，不以今日价格静默补写历史。

- 第一轮 82 项相关测试通过；第二轮 152 项通过，完整类型检查通过。第二轮重复 import 的 lint 错误已修复并单独复查通过；测试数量存在重叠，不合计为唯一用例数。

- 实测已有 Gemini 3.8 Flash 365 条带 Token 的记录中有 307 条无 cost（2026-09-06 起至核对时点）；新版修复后续真实调用的价格解析，历史缺失仍明确暴露。

- 价格核对：[Vertex AI 官方价格](https://cloud.google.com/vertex-ai/generative-ai/pricing)、[Gemini 官方价格](https://ai.google.dev/gemini-api/docs/pricing)。当前 Vertex 出站使用 global；3.8 回退采用标准 USD / 百万 Token：普通输入 1.5、缓存读取 0.15、输出含推理 7.5。活动优惠是账户 credits 返还，不直接当作每次回复费用折扣；搜索、工具、显式缓存存储、税费和账户返还不包含在模型 Token 预估金额内。

- 人民币显示使用原生 USD\_TO\_CNY=7.12，并在提示中披露固定参考汇率；非零小额不四舍五入成免费。

- 首次 Gemini 真实发送：返回 268 输入 / 143 输出（含 142 推理），原生保存 cost=$0.001475，按当前标准价逐项向上保留 6 位小数相符，累计人民币 0.3226→0.3331，无需刷新。但消息 model/provider 仍取旧 Agent 的 Terra/Azure；发现客户端消息创建与实际执行优先级不一致，补充话题模型优先修复。此为归属问题，金额本身依据实际 Gemini 用量和价格计算；历史记录不批量改写。

- 模型归属回归：原实现定向测试失败（错误记录为默认模型），修复后 96 项对话生命周期相关测试通过，完整类型检查通过。R2 真实 Gemini 回复 msg\_qBjARWxxfxV0 记录为 vertexai/gemini-3.8-flash：287 输入、111 输出（其中 110 推理），费用 $0.001264；混合话题累计 18,892 Token、$0.048052，页面自动显示 ¥0.3421，与数据库一致。

- 桌面费用行、详细汇率 / 覆盖率提示和 390px 窄屏截图均已目视核对，证据在私有 `.records/topic-cost-20260910/`。仅使用独立验收话题做两次短 Gemini 调用，没有执行工具。

- 最终双入口镜像为 `lobehub:lingshu-v2216-topic-cost-20260910-r2`；会话接口 HTTP200，PG/QStash 未重启，应用网络默认网关仍 192.168.48.1。旧生产 chat.cotticoffee.com 未发布本批。

## 2026-09-10：金额冻结验收

- 默认开启每话题 10 元，管理员可在模型管理调整金额或关闭后续金额冻结；已冻结话题不自动解冻。

- 按已记录模型费用乘固定 7.12 汇率计算，含历史；复制记录、非 assistant 及其他用户费用排除，缺失费用不伪造。

- 原生话题费用更新后检查，下一模型调用前再次检查；最后一次和在途并行请求可能超额。

- 15 项定向检查通过（数据库金额边界、费用更新后冻结、隔离、开关，路由权限，Token 守卫）；完整类型检查通过。另有原生压缩相关 235 项测试通过，统计有重叠，不累加。

- 当前只提供平台统一额度；用户独立额度为后续扩展。Gemini 3.8 Flash / Vertex 校正后 100 万独立 Token 上限，原生压缩配置无二开覆盖。

- 实际环境：管理员保存接口 200、数据库配置 `enabled=true, limit_fen=1000`；真实 PostgreSQL 回滚事务中 ¥14.24 费用入库生成 budget 冻结，回滚后测试用户零残留。管理员页面已截图核对；普通账号访问管理页被拒绝。两个域名认证健康接口均 200，应用同镜像，PG/QStash 启动时间保持不变。
