# COTTI 产品统一登录（灵枢 OIDC Provider）

## 范围

灵枢 AI 作为 COTTI 产品统一身份提供方，灵境 AI、灵演 AI 与 ComfyUI 作为独立的 OIDC confidential client。用户在灵枢已有有效登录会话时，进入这些产品可自动完成第一方授权；其他官方客户端和第三方 OAuth 应用保持原确认流程。

本二开不新增数据库表或迁移，也不改变飞书登录的授权入口。飞书返回真实邮箱时将其标记为已验证，灵枢再通过标准 `email_verified` claim 传递给下游；飞书未返回真实邮箱时继续使用未验证的占位邮箱，禁止用于统一登录账号绑定。

## 客户端与回调

| 产品    | Client ID             | 回调地址                                                                                                                              |
| ------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 灵境 AI | `cotticoffee-nano`    | `https://nano.cotticoffee.com/api/auth/callback/cotti-sso`、`https://nanodev.cotticoffee.com/api/auth/callback/cotti-sso`             |
| 灵演 AI | `cotticoffee-ppt`     | `https://ppt.cotticoffee.com/api/auth/oauth2/callback/cotti-sso`、`https://pptdev.cotticoffee.com/api/auth/oauth2/callback/cotti-sso` |
| ComfyUI | `cotticoffee-comfyui` | `https://comfy.cotticoffee.com/oauth2/callback`                                                                                       |

两端均使用 Authorization Code + PKCE、`client_secret_basic` 和 `openid profile email offline_access`。两组客户端密钥必须独立生成，禁止混用：

- 灵枢 `COTTI_SSO_NANO_CLIENT_SECRET` = 灵境 `COTTI_SSO_CLIENT_SECRET`
- 灵枢 `COTTI_SSO_PPT_CLIENT_SECRET` = 灵演 `COTTI_SSO_CLIENT_SECRET`
- 灵枢 `COTTI_SSO_COMFYUI_CLIENT_SECRET` = ComfyUI 认证代理 `client-secret`

## 二开触点

1. `packages/env/src/auth.ts` 与 `.env.example`：声明两组可选服务端密钥；缺少密钥时对应客户端不注册。
2. `src/libs/oidc-provider/config.ts`：注册两个静态第一方 confidential client，不开放动态回调地址。
3. `src/features/Auth/OAuthConsent/cottiClients.ts`、`Login.tsx` 与 `Consent/index.tsx`：只对两个 COTTI 客户端自动确认登录和内置授权；桌面端、Market 与第三方客户端不受影响。
4. `src/libs/next/proxy/define-config.ts`：OIDC discovery、JWKS、token、userinfo、introspection、revocation 和 logout 协议端点绕过 Better Auth 页面会话门禁；各协议端点仍由 OIDC Provider 校验客户端或 token。
5. `src/libs/oidc-provider/http-adapter.ts` 与 `provider.ts`：Next.js 对外统一挂载在 `/oidc/*`。Provider 可配置端点显式保留 `/oidc`；只对库中固定的 `.well-known` discovery 路径剥离一次挂载前缀，确保 discovery 宣告的所有端点仍指向外部 `/oidc/*` 地址。
6. `src/libs/better-auth/sso/providers/feishu.ts` 与 `src/libs/oidc-provider/provider.ts`：真实飞书邮箱的验证状态进入 OIDC `email_verified` claim。

## 发布顺序与门禁

1. 在灵枢运行环境写入两组密钥并部署灵枢。
2. 验证 `/oidc/.well-known/openid-configuration` 与 `/oidc/jwks` 返回 JSON，issuer 与当前环境域名一致，不能跳转到登录页。
3. 配置并部署灵境 dev；使用同一 Nano 密钥完成真实登录。
4. 配置并部署灵演 dev；使用同一 PPT 密钥完成真实登录。
5. dev 双端验收通过后，按相同顺序发布生产；生产密钥应与 dev 分离。

缺少任一密钥、下游密钥不一致、issuer 错误或回调地址不匹配都属于发布阻断项。密钥不得提交到 Git、构建参数、日志或验收输出。

## 升级回放与回退

升级时优先保留 OIDC Provider 官方实现，只重放上述静态客户端、COTTI 自动确认边界、协议公开路由与 Feishu email verification 四组小改动。回退时移除两个运行时密钥即可停止注册对应客户端；无需回滚数据库。若下游已切换到统一登录，应先恢复下游原登录方式，再移除灵枢客户端。

## 2026-09-03：`cotti.ai` Portal 身份域接入灵枢

### 目标与隔离边界

- `chat.cotti.ai` 从 Cotti AI Portal 进入时，复用 `auth.cotti.ai` 的手机或邮箱白名单登录状态。
- 新身份域的页面与跳转不出现 `cotticoffee.com`。
- `chatdev.cotticoffee.com` 继续使用原实例、原 `APP_URL`、飞书和邮箱登录配置；本次不修改其 Nginx upstream 或运行环境。
- 新旧域名使用两个独立的应用实例。它们可以共用同一个灵枢构建镜像和业务数据库，但必须使用各自的认证基址与 provider 配置。

### 二开触点

1. `src/libs/cotti-ai-sso/entry.ts` 与 `src/app/(backend)/auth/cotti-ai-entry/route.ts`
   - 只对严格主机名 `chat.cotti.ai` 启用服务端统一登录入口；直接访问产品域名与从 Portal 跳转使用同一条链路。
   - 已有灵枢会话时直接回到目标页面；没有灵枢会话时，由服务端调用 Better Auth 生成 OAuth state/PKCE Cookie，再以 `302` 进入 `auth.cotti.ai`，不会先渲染灵枢登录页。
   - `returnTo` 仅接受当前站点的相对路径，拒绝协议相对 URL、反斜杠、控制字符和认证页面回环，避免开放重定向与登录循环。
2. `src/libs/next/proxy/define-config.ts`
   - `chat.cotti.ai` 的匿名受保护请求直接转到 `/auth/cotti-ai-entry`，并保留当前产品内路径与查询参数。
   - `chatdev.cotticoffee.com` 仍沿用原 `/signin?callbackUrl=...` 行为，不进入新身份域。
3. `src/features/Auth/SignIn/useSignIn.ts`
   - 保留旧版本登录页的客户端自动发起逻辑作为兼容兜底；存在 OAuth `error` 时不自动重试，确保错误恢复页可见而不会循环。
   - `chat.cotti.ai` 的 callback 收敛为 `/`，丢弃历史入口中指向 `chatdev.cotticoffee.com` 的 callback；其他域名保持原 callback 行为。
4. `src/app/(backend)/auth/cotti-ai-entry/route.test.ts`、`src/libs/cotti-ai-sso/entry.test.ts`、`src/libs/next/proxy/define-config.test.ts` 与 `src/features/Auth/SignIn/useSignIn.test.ts`
   - 回归覆盖已有会话直达、匿名服务端 OAuth、state Cookie 透传、站内回跳校验、严格域名隔离、受保护路由直达，以及错误页不重复发起授权。
5. `src/libs/better-auth/sso/providers/generic-oidc.ts`
   - 显式设置 `authentication: 'basic'`，使 token 兑换使用 HTTP Basic 客户端认证，与 Portal 中 `cotti-chat` 的 `client_secret_basic` 注册方式一致。
   - 若缺少该设置，Better Auth 默认使用 `client_secret_post`，Portal 会返回 `invalid_client`，灵枢页面最终显示 `invalid_code`。
6. `src/libs/better-auth/sso/providers/generic-oidc.test.ts`
   - 回归锁定 Generic OIDC 的 token 客户端认证方式为 `basic`。

### OAuth 客户端与运行环境

Portal 身份服务中注册的客户端：

| 产品    | Client ID    | 回调地址                                               |
| ------- | ------------ | ------------------------------------------------------ |
| 灵枢 AI | `cotti-chat` | `https://chat.cotti.ai/api/auth/callback/generic-oidc` |

`chat.cotti.ai` 独立实例使用以下关键配置。密钥只从安全环境注入，禁止写入 Git、本文档、镜像构建参数或日志：

```dotenv
APP_URL=https://chat.cotti.ai
AUTH_SSO_PROVIDERS=generic-oidc
AUTH_GENERIC_OIDC_ID=cotti-chat
AUTH_GENERIC_OIDC_SECRET=<从安全环境注入>
AUTH_GENERIC_OIDC_ISSUER=https://auth.cotti.ai/api/auth
AUTH_DISABLE_EMAIL_PASSWORD=1
AUTH_ENABLE_MAGIC_LINK=0
```

### 部署、验收与回滚

1. 新建 `chat.cotti.ai` 独立容器和端口，确认健康后才切换该域名的 Nginx upstream。
2. 验证未登录访问会进入 `auth.cotti.ai`；Portal 已登录时应无感返回灵枢首页；全链路不得跳转到 `chatdev.cotticoffee.com`。
3. 单独验证 `chatdev.cotticoffee.com` 仍展示并使用原登录方式。
4. 回滚时仅将 `chat.cotti.ai` upstream 切回其回滚容器或旧端口并停止新实例；不操作 `chatdev.cotticoffee.com` 实例。

2026-09-03 dev 当前部署记录：

- 镜像：`lobehub:cotti-ai-portal-sso-basic-20260903`（镜像 SHA256 前缀 `93cf93f183cb`）
- 容器：`lobehub-cotti-ai-hotfix`
- 宿主机 upstream：`127.0.0.1:3221`
- Nginx：`/etc/nginx/conf.d/chat.cotti.ai.conf`
- 旧域名容器：`lobehub-v228-stage0` / `127.0.0.1:3210`，未修改
- 上一版新域名候选：`lobehub-cotti-ai` / `127.0.0.1:3220`，仅保留作即时回滚，不承载流量
- 目标测试：`bunx vitest run src/libs/better-auth/sso/providers/generic-oidc.test.ts src/features/Auth/SignIn/useSignIn.test.ts`，31 项通过
- 文件检查：`bun run check` 覆盖上述 4 个实现与测试文件，lint clean 且 31 项测试通过

### 2026-09-03 产品域名服务端直达

- 现象：匿名访问 `chat.cotti.ai` 先显示灵枢登录页，再由浏览器端 JavaScript 自动点击统一登录，产生可见过渡页；从 Portal 跳转和直接输入产品域名的体验也不一致。
- 根因：受保护路由的 Better Auth 中间件固定重定向 `/signin`，统一 OIDC 只在登录页挂载后的 `useEffect` 中发起。
- 修复：严格限定 `chat.cotti.ai`，把匿名受保护请求送到 `/auth/cotti-ai-entry`；该入口在服务端检查灵枢会话、生成 OAuth state/PKCE、保留 `Set-Cookie` 并直接 `302` 到 `auth.cotti.ai`。Portal 仅作为产品导航，不再是统一登录能力的必要入口。
- 隔离：没有修改 `chatdev.cotticoffee.com` 的容器、Nginx、登录页面或 provider 配置；非 `chat.cotti.ai` 主机访问新入口会回到原 `/signin`。
- 隐私模式：支持。邮箱验证链接必须在发起登录的同一隐私窗口中打开，否则普通窗口与隐私窗口的中心会话、OAuth state Cookie 不共享。
- 回归门禁：`bun run check` 覆盖 8 个相关实现与测试文件，lint clean，51 项测试通过。
- 代理适配：Next.js standalone 在容器内把 `nextUrl` 重写到 `0.0.0.0:3210`；实际产品域名从 Nginx 固定覆盖的首个 `X-Forwarded-Host` 解析，之后仍执行 `chat.cotti.ai` 精确匹配。
- 镜像安全：`Dockerfile` 在组装最终镜像前移除 `.next/standalone/.env*`。运行时密钥继续由容器环境注入，镜像验收必须确认 `/app/.env*` 不存在。
- 当前镜像：`lobehub:cotti-ai-server-sso-entry-secure-20260903-2120`（SHA256 前缀 `fc45b7274e5f`）。
- 当前容器：`lobehub-cotti-ai-hotfix` / `127.0.0.1:3221`；旧容器保留为 `lobehub-cotti-ai-hotfix-rollback-20260903-2124`，使用已移除 `/app/.env*` 的 `lobehub:cotti-ai-portal-sso-basic-secure-rollback-20260903`，处于未启动状态。
- 线上验收：`chat.cotti.ai/` → `302 /auth/cotti-ai-entry?returnTo=%2F` → `302 auth.cotti.ai/api/auth/oauth2/authorize`；回调固定为 `https://chat.cotti.ai/api/auth/callback/generic-oidc`，响应包含 HttpOnly/Secure/SameSite=Lax 的 Better Auth state Cookie。

### 2026-09-03 token 兑换热修复

- 现象：Portal 授权成功返回后，灵枢显示 `ErrorCode: invalid_code`。
- 服务端原始错误：`client registered for client_secret_basic cannot use client_secret_post`。
- 根因：Better Auth Generic OIDC 默认采用 `client_secret_post`，与 Portal 客户端注册不一致。
- 修复：Generic OIDC provider 显式使用 `authentication: 'basic'`；不修改 Portal 客户端、不降低客户端认证要求，也不影响飞书 provider。
- 真实链路验收：复用白名单邮箱现有 Portal 会话发起 Authorization Code 流程，Portal `302` 返回 `chat.cotti.ai/api/auth/callback/generic-oidc`，灵枢成功兑换授权码、设置会话 Cookie，并 `302` 回到 `https://chat.cotti.ai/`；热修容器日志无 `invalid_code`、`invalid_client` 或 `client_secret_post` 错误。

## 2026-09-04：登录准入人员编辑与 7 天会话

- 此能力默认关闭。仅在具备完整 `cotti_ai_auth` schema 的开发环境设置 `COTTI_AI_ACCESS_MANAGEMENT_ENABLED=1`；生产环境不设置该变量，服务端不会查询身份 schema，界面也不会展示 `cotti.ai` 成员管理。
- 平台管理沿用现有 “登录准入” 列表，不增加新的页签。`cotti.ai` 成员行新增编辑操作，可修改姓名、手机号、邮箱和备注；姓名必填，手机号与邮箱至少保留一项。
- 同一成员同时配置手机号和邮箱时，两种身份仍绑定到同一个 `cotti_ai_auth.user`。编辑按成员 ID 精确更新，不通过新身份猜测或合并账号。
- 新邮箱或手机号若已属于其他白名单成员或统一账号，接口返回冲突并回滚整个修改；不会自动合并两位用户。
- 身份变化会同步统一账号、将新身份标记为待验证，并撤销该账号现有中心 Session、OAuth Access Token 与 Refresh Token；仅改姓名或备注不强制退出。
- `src/libs/better-auth/define-config.ts` 显式声明应用会话为 7 天，与 Better Auth 原默认值一致。该声明不会改变现有 `cotticoffee.com` 的实际有效期，只防止未来依赖升级改变默认行为。
- dev 部署镜像：`lobehub:cotti-access-edit-7d-20260904-1102`（SHA256 前缀 `6beff275befb`）。`chatdev.cotticoffee.com` 使用 `lobehub-cotti-access-dev-candidate` / `127.0.0.1:3222`，保留飞书与原邮箱登录；`chat.cotti.ai` 使用 `lobehub-cotti-access-chat-candidate` / `127.0.0.1:3223`，继续只接 Portal OIDC。
- 回滚实例 `lobehub-v228-stage0` / `127.0.0.1:3210` 与 `lobehub-cotti-ai-hotfix` / `127.0.0.1:3221` 均保持运行但不承载对应 Nginx 流量，可分别即时切回。
- 数据库迁移 `cotti-portal/migrations/003_fix_access_members_phone_constraint.sql` 修正首版手机号 CHECK 约束的双反斜杠转义错误，明确接受代码产生的 `+861xxxxxxxxxx` E.164 值；否则管理端编辑手机号会在事务中被 PostgreSQL `23514` 回滚。
