# v2.2.18 二开合并状态

日期：2026-09-24。一次完成必要合并，再统一验收；完整候选已于 2026-09-24 经授权部署至两个开发域名，生产未切换。

## 保留与适配

完整清单包含原 DEV 核对项及 v2.2.17 上线后新增功能。保留登录与准入、飞书双入口和资料连接器、模型下线与用户组渠道、计费金额控制、管理全览、上传下载、话题交互、官方和自建沙箱、委派工具、静态资源兼容、图片预览等实际二开。不恢复已停用的上下文长度冻结，不接入飞书机器人或桌面电脑操作。

上游本轮重构的工具规则、发现池和连接器构建使用新版共享实现，在明确扩展点保留二开行为，不整段搬回旧装配代码。相关共享工具测试 21 项通过；客户端和服务端对应回归纳入应用检查。这是实现替换，未有意删除现有功能，统一验收前仍需向用户说明。

原计划删除的 typed Error 序列化补丁保留：仅使用原生归一化时，冻结错误的类型与说明丢失回归失败。最终先保留错误字段，再调用原生归一化，回归通过。

## 自动验证

- 应用相关 189 个测试文件首次合并检查：2,491 通过、9 失败、3 跳过。9 个失败均已定位为新版接口对应的测试夹具问题，修复后针对这 5 个文件重跑 168 项全部通过；未声称重新跑过全仓所有测试。
- 十个共享包相关测试合计 781 项通过。Gateway 专项 78 项通过。后续上传等检查 21 项通过。上述数字存在交叉，不相加作为独立用例数。
- 生产源码 495 文件 lint 通过；全仓类型检查通过；完整应用与 Docker 镜像构建成功。
- 开发、生产数据库副本升级及重复运行官方自动迁移器通过。空库首次安装存在可在官方原版复现的历史迁移顺序错误，详见部署说明。
- 无外网的隔离应用启动成功：登录页、认证配置、空会话查询以及抽查的 3 个登录 JS 资源均 HTTP 200。无真实用户登录、邮件、模型或定时任务调用。

本机详细证据位于 `.records/v2218-full-merge/` 和私有备份目录，不提交包含运行配置或用户数据的证据。

## 构建产物

镜像：`lobehub:lingshu-v2218-full-acceptance-20260924-r2`

镜像 ID：`sha256:823336c14091938052303050ee6bf68054c3a6f2d054ba767a74f19775e85df9`

r2 包含验收发现的全览缓存刷新、发送前拒绝的计费完整性修正；均有回归测试和部署后浏览器验证，详见后续验收记录。

## 尚未验收

已完成双域名共享登录与数据、基础对话、模型选择、管理页面、自建与云端沙箱执行及附件下载、预算触顶及解冻。第三轮已补齐飞书重新授权及显式连接器读取、浏览器 / 服务端委派、单任务 QStash 回调、自建沙箱以及 82.2 MiB Excel 上传进度；见 [第三轮记录](../deploy/v2218/acceptance-round3-20260924.md)。全局调度扫描周期、分享 / PDF 等外围回归、带新写入的旧应用回退仍需核对。

具体结果见 [首次发布记录](../deploy/v2218/acceptance-release-20260924.md) 与 [登录后验收及修正](../deploy/v2218/acceptance-followup-20260924.md)。当前仍是集中验收版本，生产未切换；不得标记全量验收完成。
