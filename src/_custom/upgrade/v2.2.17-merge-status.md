# v2.2.17 剩余二开合并记录

日期：2026-09-13。源码合并及开发环境发布完成；第二轮 13 项核心业务验收通过。蓝版真人登录、全部模型组合和长期稳定性不在本轮通过范围。

## 边界与基线

- 官方 v2.2.17：`787e3d20aa`；旧最终二开：`e6e4c774fb`。
- 最小版：`7324da2c0c`，标签 `milestone/v2.2.17-minimal-accepted-20260913`。
- 本轮分支：`upgrade/v2.2.17-customizations`。
- 仅 chatdev.cotticoffee.com 使用本轮镜像。chat.cotti.ai 继续原 v2.2.16，生产 chat.cotticoffee.com 未修改。
- 飞书机器人不启用、不接入；保留普通 / 蓝版飞书登录与个人资料连接器。没有发送外部消息。

## 实际保留的二开

逐项编号、源码锚点与必要性见合并前清单的 55 条核对记录。主要范围：

- 平台管理：概览、合规审计、模型池与显示名、即时保存、VIP、人员单行管理、Agent 权限、平台 / 用户 / 话题额度、首页通知。
- 全览：跨用户搜索、费用 / 日期排序、原生只读会话展示、模型及费用明细、冻结 / 解冻、临时额度。
- 成本与模型：累计费用、缓存 / 分档计价、下线模型保护与任务迁移、换题提示、命名加描述、预算预留；上下文压缩继续官方逻辑。
- 首页和交互：灵枢 AI 品牌、Chat/Agent/Task 模式、模型快捷入口、小尺寸人物、联系管理员、安全提醒。
- 工具与任务：原生搜索优先及 SearXNG 回退、个人飞书资料、Composio 配置、连续三次未查看暂停、附件隔离与按需读取、沙箱附件指纹。
- 内容展示：分享入口、PDF 兼容、GPT Image 2.5 Flare、强制单张、隐藏视频、320px 图片预览。

## 官方覆盖与适配

- 保留原生话题 ID 传递，撤回重复的 transport、Web API 和 bot topicId 补丁。
- 首页在官方 speech 共同布局上缩小人物；不回填旧绝对定位结构。
- 保留官方千问 3.8、豆包聊天、话题推理选择、懒加载及压缩实现。
- 企业默认推理强度尊重官方显式空配置 `{}`，对应官方新增回归用例通过。
- 不复制旧迁移快照、旧部署脚本、临时生产诊断与旧双域名 SSO 补丁。新环境继续独立 QStash 与 RustFS，不回填旧队列 / OSS 环境专用补丁。

## 数据和发布

- 追加迁移 `0163_lingshu_remaining_customizations`：审计风险、审计查看记录、首页通知表；生成快照并保留历史，未改旧库迁移。
- 新副本迁移记录共 172 条；新库备份 `/opt/backups/lingshu-v2217-before-full-20260913` 已校验可列出恢复目录。
- 镜像 `lobehub:lingshu-v2217-customizations-20260913-r1`。
- digest：`sha256:826719eddde6979e4d47cef660a39f38d27abde2ab45294bde09d85eb9674ec9`。
- 当前容器 `lingshu-v2217-app-dev`；最小版容器 `lingshu-v2217-app-minimal-accepted` 已停止保留，禁止清理。
- 独立 PG、Redis、QStash、对象存储及旧 cotti Nginx 哈希检查全部通过。两个版本数据库自首次切换起独立，不持续同步。

## 已完成验证

- 全仓类型检查、正式镜像构建、改动文件 lint 与关联测试通过。首次失败已按原因修复后定向复验；并非声称运行整个仓库测试套件。
- PGlite 迁移、模型策略、成本保护、任务和话题相关回归通过。记忆集成测试显式给测试用户足够额度，避免成本保护干扰记忆语义测试；没有改变运行环境额度。
- Chrome 9222 管理员会话有效；实际截图检查：首页、平台概览、人员单行列表、模型管理及默认 10 元额度、全览跨用户费用排序、原生详情与模型 / Token / 费用面板。
- 搜索 `SC课程教学` 能找到实际标题以 “洛阳科技职业学院……” 开头的已冻结话题。旧简写 `SC课程教学竞赛指导` 不是副本中的完整连续标题，不能据此判定缺失数据。
- 页面验证采用只读检查，没有修改其他用户权限、模型上下线或业务话题额度。

## 第二轮实际业务验收（2026-09-13）

私有记录：`.records/reports/v2217-full/20260913-round2/`。所有截图实际打开查看，没有上传第三方平台。

- 真实 Gemini 3.8 Flash 对话、累计用量、自动标题和描述通过。
- 天气新问题触发换题弹框，默认焦点在 “新建话题并发送”，确认后创建独立话题；模型自带搜索返回结果。
- 专用话题设 1 分钱临时限额，下一次发送被预算守卫冻结，费用未增加；恢复继承额度并解冻后真实对话成功。
- 云端沙箱 3 步运行 Python 并写读文件，计算及读回均为 83810205。网页对话为客户端执行链；定时任务另行验证了服务端队列。
- 专用任务 T-22 通过独立 QStash 真实投递，3 条定时结果 completed、3 个 operation 为 done。第四次触发后 paused 且总次数保持 3，暂停原因为连续未查看。
- 飞书资料通过已有授权真实读取用户提供的 Wiki；不接机器人、不写外部文档。本轮未穷举自然语言意图选择。
- PDF 实际生成 63838 字节，渲染及文本提取均验证中文、模型计算与沙箱数字。
- Azure 的 gpt-image-2.5-flare 实际生成一张图，页面预览宽 318.4px，缩略图正常；Nano Banana2 选择保留。
- 全量镜像刷新后的分享页面与历史附件正常。省略 Cookie 的分享 API 返回 200；同样无 Cookie 的全览 API 返回 401。
- 结束时专用任务保持暂停，话题临时额度已清除且解冻，用户原有 100 元额度不变、平台默认 10 元不变；恢复 Chat 模式和原 Nano Banana 图片选择。

### 验收范围与观察项

- 中心调度保持官方每 10 分钟扫描，本轮对指定任务投递 QStash 消息加速验证，没有改调度频率。测试用的每分钟 cron 在现有 UI 概括为 “每小时”，不以该文本作为精确计时证据。
- 预算拒绝产生不带 usage 的错误记录，因此话题可显示 “统计不完整”；真实已记录费用仍正常累加，不代表该次被拒绝调用产生了费用。
- 蓝版飞书真人登录继续按用户安排；没有将未执行项目列为通过。
- 本轮未修改业务代码，无需重新发布镜像。下一步为用户业务验收及后续稳定性观察，生产发布仍不在本次授权范围。

原存储缺失的一张历史 PNG 为最小阶段已确认的源文件缺失，不属于本轮合并新增问题；原 OSS 未修改。验收截图和详细日志保留在私有 `.records/v2217-merge-audit/`，不对外发布。

## 首页修复发布（2026-09-13）

- 修复提交：`ac6a5e0ae0`。首页 `topicModelScope=false` 统一约束模型选择器、推理强度和模型列表切换的读写，避免残留旧话题状态干扰新话题。普通会话继续保留官方话题模型及推理配置。
- 归因边界：官方组件存在读取 activeTopic 的逻辑，但未在未修改的官方完整部署中复现；不能据此断言官方产品必然有同样 bug。本次明确修复的是二开首页的配置隔离遗漏。
- 首页默认人物图改为同域 `/avatars/lingshu-home-portrait.webp`，保留 152px 尺寸和飞书联系链接；自定义图片加载失败时回退本地图片。未复现用户当时的外部图片失败，未将原因归为已证实的网络封禁。
- 镜像：`lobehub:lingshu-v2217-home-fixes-20260913-r1`，digest `sha256:4161f5ee5a429af8bc56623a5acfe79f27ee400ff67f33489642e090571e6a2a`。仅发布 chatdev。
- 旧全量容器保留为 `lingshu-v2217-app-before-home-fixes`，停止且禁用自动启动；最小版备份继续保留。无数据库结构变更。
- 模型回归 3 项、人物图回归 2 项及全仓类型检查通过，已记录修复前失败。正式构建和部署后隔离检查通过。
- Chrome 实际验证：从旧验收话题经页面返回首页，选择 COTTI - 快速 / 低，发送后新话题 `tpc_zVn8MSWEMl7D` 回复 “验收成功”。数据库记录模型为 `vertexai/gemini-3.5-flash-lite`，话题 `thinkingLevel=low`；647 Token，估算 ¥0.0014。人物图同域加载成功，宽 152px，截图已打开核验。
- 私有发布与验收记录：`.records/home-fixes-20260913/`。未上传第三方；chat.cotti.ai 原容器、镜像及 Nginx 配置保持不变。

## 自建沙箱容量管理（2026-09-14，仅开发环境）

- 按用户确认，平台管理新增自建沙箱会话数量设置，自动保存；即时限制新建会话，降低上限保留正在使用的沙箱，不重启 Worker。
- PostgreSQL 持久化配置和准入名额，新增迁移 0164；已有官方 Market 调用不改变。详细边界及实测见 [容量管理说明](../deploy/onlyboxes-trial/capacity-settings-design.md)。
- 相关检查 28 项、管理员权限 2 项通过，全仓类型检查和正式构建通过；独立 PostgreSQL 24 请求并发测试与真实 OnlyBoxes 动态加减名额测试通过。
- 开发镜像 `lobehub:lingshu-v2217-sandbox-capacity-20260914-r1`，digest `sha256:f124bcd4ded68df1e9e8db194faf6c96266220b71d7b4e3567972f1299060fbe`。发布前备份开发数据库，旧容器保留为 `lingshu-v2217-app-before-sandbox-capacity`，停止并禁用自启。
- 自建沙箱尚未向真实用户启用，管理页明确显示此状态。本轮没有切换官方沙箱，也没有增加并列运行环境入口；生产及 chat.cotti.ai 未改动。
- Chrome DevTools MCP 页面验收：开发上限设为 5，回车保存及刷新持久化通过；输入 0 被前端拒绝，数据库仍为 5。截图已打开核验，发布后运行隔离检查通过。

## 执行环境菜单精简（2026-09-14，仅 chatdev）

- 按用户纠正，恢复官方文案；网页版只保留 “云端沙箱 / 在临时云端沙箱中运行”。去掉无设备、自动设备、设备管理、桌面端下载和不可用的自建沙箱占位。原桌面客户端能力保留。
- 不改历史 Agent 配置。可切换但保存了其他运行目标的网页版按钮显示 “执行环境”，供用户明确选择可用沙箱；不静默启用第三方执行环境。普通 Chat 模式不改变。
- 之前修改官方文案、加入灰色占位的方案已按用户要求撤回，不能将菜单占位或容量管理完成表述为自建沙箱实际接入。
- 自建沙箱仍待：按选择路由、正式服务部署、文件读写与导出、长任务租期验收。完成后才加入可选入口。
- 已发布开发镜像 `lobehub:lingshu-v2217-sandbox-menu-simple-20260914-r1`，digest `sha256:5e910dd571b465b1b3ceb0ec5c82f9e84a793fc314c00579b07ca7f9dc63fd76`。仅 chatdev；生产及 chat.cotti.ai 未改。无数据库迁移，旧容器保留为 `lingshu-v2217-app-before-sandbox-menu-simple`。
- 代码检查、类型检查、构建和运行隔离检查通过。Chrome DevTools MCP 实际打开菜单，只显示 “执行环境 / 云端沙箱 / 在临时云端沙箱中运行”，截图已打开核验；未发送模型请求，未改既有 Agent 配置。

## OnlyBoxes 实际接入（2026-09-14，仅 chatdev）

- 自建沙箱已启用，与官方 “云端沙箱” 并列；去掉此前不可用占位状态。默认仍为云端，用户自行选择自建。
- 话题创建时保存路由，刷新不丢失；切换时确认新开话题。首页普通发送、网关执行、首页任务及来源话题创建的任务传递沙箱选择；服务端命令、技能、文件同步和导出统一路由。不新增数据库迁移，不修改官方压缩、模型、成本规则。
- 平台名额即时生效，开发上限 5；满额拒绝新建、不自动转云端，现有会话继续。2 CPU / 2 GiB，闲置 15 分钟；命令租期按超时加余量保护。
- Console / Worker 0.11.0 独立服务，自建沙箱专用网络隔离内网和元数据，允许公开联网与安装依赖。原生任务取消不能保证停止容器进程，补充适配器内进程组终止并实测取消后不再写文件。Worker 重启恢复未过期会话也已实测，修正旧判断。
- 定向回归、类型检查和正式镜像构建通过。Chrome 真实快速模型调用、文件导出下载及切换提示通过；测试费用约 ¥0.0685。仅使用现有开发环境模型连接，没有修改其他用户业务话题。
- 发布 lobehub:lingshu-v2217-onlyboxes-20260914-r3；开发数据库备份已验证，旧容器 lingshu-v2217-app-before-onlyboxes 停止保留。chat.cotti.ai 与生产未修改。
- 部署、源码锚点及验收边界见 [OnlyBoxes 开发环境接入](../deploy/onlyboxes-dev/README.md)。下一阶段先验收真实业务附件和多任务并发，再考虑生产启用。

## 会话与智能模式模型一致性（2026-09-14，仅 chatdev）

- 修复二开模式模型池的衔接遗漏：模式保存在 Agent，模型由各话题独立保存；进入旧话题时也校验当前模式可用模型，不可用才替换成该模式默认模型，仍可用的用户选择保持不变。
- 等待话题模型加载后再切换，避免将 Agent 默认值误判为已更新的话题配置；防止手动切换的中间模型写入被旧模式反向覆盖。复用已有模型选择与话题保存接口，没有数据库迁移。
- 修复前回归测试已确认旧话题缺少校验、加载中误报切换成功；实际页面明确点击对话转智能原本有效，问题不归因于每次点击都失败。
- 相关 15 项回归、lint、全仓类型检查和镜像构建通过；Chrome DevTools MCP 通过本人验收话题复现旧行为，发布后打开旧话题自动切为智能默认模型，刷新及数据库持久化通过。
- 镜像 `lobehub:lingshu-v2217-mode-model-20260914-r1`，digest `sha256:8887334dbeb48c9e118e0d8dc7e4b45ca2641cc8d439af7c14fcb09ca22c4446`。开发数据库备份已验证，旧容器 `lingshu-v2217-app-before-mode-model` 停止保留，保留 OnlyBoxes 网络和配置。仅发布 chatdev；生产及 chat.cotti.ai 未改，运行隔离检查通过。私有证据 `.records/mode-model-fix/`。
- 一次真实请求回复 `MODEL_MODE_OK`，消息记录为 `vertexai/gemini-3.8-flash`，与界面 COTTI - 专业一致；本轮新增 21,213 Token，平台估算约 ¥0.115。下一步继续自建沙箱业务附件与多任务并发验收。

## 双域名统一服务与全览搜索修复（2026-09-14）

- 用户明确最终边界为两个域名、一套服务。此前独立库仅用于升级验收；现已完成备份、三方数据比较、隔离演练及正式合并，保留双方数据。
- 两个同镜像应用入口共享数据库、OSS、Redis、QStash、内部任务回调及 OnlyBoxes 容量配置，保留各自域名和认证回调。旧调度暂停，避免重复执行。生产 chat.cotticoffee.com 本轮未改。
- 全览取消粘贴时阻止默认行为并立即跳转的处理，完整链接留在输入框，提取 ID 搜索，回车打开详情；输入长度上限 2,048。新输入搜索词自动包含冻结话题，用户可随后手动筛选。
- “比赛讲稿优化打磨” 来自此前另一数据库，且处于冻结状态；数据统一与搜索筛选修复共同解决该场景。权限和查看审计保持原有约束。
- 定向回归修复前失败、修复后通过；5 项检查、lint、类型检查、构建和运行配置核对通过。浏览器验收待用户完成登录，不标记为已通过。
- 当前镜像、数据合并统计、备份及回退边界以 [双域名共享服务说明](../deploy/cotti-v2217/README.md) 为准。此前各节 “仅 chatdev / 独立资源” 描述当时的阶段范围，不代表当前部署。

## Astraflow 压测组（2026-09-14）

- 新增用户组渠道边界和人员管理单行分组字段；创建空的压测组，开放 Astraflow 的四个聊天模型与 GPT Image 2.5 Flare。现有用户不自动入组，原渠道 Sol 下线规则不变。
- 组内辅助模型、默认模型、聊天及图片可见范围与实际调用权限保持一致；全局管理配置与组内配置分离。新增迁移 0165，无历史业务数据回填。
- 当前部署、代码边界和后续费用核账范围见 [压测组说明](../deploy/v2217/astraflow-pressure-group.md)。

## 网页智能模式默认云端沙箱（2026-09-14）

- 原因：二开精简执行环境菜单后，仍沿用官方网页端未设置目标解析为 `none` 的逻辑。修复补齐实际配置保存，不只替换显示文字。
- 网页原生 Agent 切换智能时，先通过原有权限感知接口保存 `sandbox`；保存失败不继续开启智能。打开已处于智能模式的页面时，同一规则修正未设置或遗留 `none`。
- 普通 Chat 不启用环境，已保存的沙箱、桌面 / 远程设备和固定 Workspace 策略保留；不修改话题的 `sandboxProvider`，所以自建沙箱话题不会被改成官方云端。
- 官方通用执行目标解析器保持原状，不批量回填 Agent 或任务数据。新增回归在修复前失败，修复后 19 项测试通过，类型检查及正式构建通过。
- 两域同步发布镜像 `lobehub:lingshu-v2217-agent-sandbox-default-20260914-r1`，digest `sha256:3f45a8a23e1c41fb03de49b4ec94bcdd9b7be4189d76a8581647c14e700dba81`。旧应用停止保留，共享数据库与模型目录不变。
- 私有备份 `/opt/backups/lingshu-agent-sandbox-default-20260914/`，验收及发布记录 `.records/agent-default-sandbox/`。本次不修改生产 chat.cotticoffee.com。
- Chrome DevTools MCP 实测：既有专用验收 Agent 原 `executionTarget` 未设置，打开智能页面后实际保存为 `sandbox`，显示云端沙箱。首页切换智能后恢复原 Chat 模式；没有发送模型请求或新增业务话题。截图保留私有记录。

## 账号菜单完整邮箱（2026-09-14，2026-09-15 已发布）

- 账号头像菜单保留昵称，第二行优先显示完整邮箱，区分不同邮箱域名但用户名相同的账号；没有邮箱时回退姓名或用户名。退出后不显示残留邮箱。
- 仅调整 UserInfo 的账号标识选择器，不改变会话署名、首页称呼、账号资料及登录规则。邮箱仅在本人的账号菜单展示。
- 原显示优先级回放触发回归失败；新逻辑定向测试 18 项通过，lint 通过。发布时必须与其他已确认变更一起同步到 chat.cotti.ai 和 chatdev.cotticoffee.com。

## 全览默认筛选（2026-09-15，已发布）

- 无显式 URL 筛选条件时默认全部话题、最近更新排序；无效筛选值同样回退到该组合。保留已明确指定的状态、排序和页码。仅修改前端默认值，不调整计费及冻结逻辑。
- 定向回归覆盖默认值、无效参数、显式筛选和原有链接检索；发布需双域名同步。

## 用户组模型管理页签（2026-09-15，已发布）

- 模型管理新增默认组及已有用户组页签，复用同一 Chat / Agent 编辑器。压测组明确标注 Astraflow，组内名称、排序、VIP、可见模型及默认模型即时保存；候选模型按渠道过滤，缓存按组隔离。
- 管理员接口增加可选 groupId，保存只更新 cotti\_user\_groups.model\_display。保留图片模型配置，不改变渠道凭据，不新增数据库字段或迁移。默认组不能启用专属渠道模型。
- 复用原模型下线 / 引用迁移类，增加组内用户范围，预览及事务迁移覆盖组内 Agent、话题和未结束任务；跨渠道替代被拒绝。下线映射存入组配置，组辅助模型引用下线模型时同步替换。旧客户端、排队及运行中任务后续请求按当前用户组解析替代模型。
- 测试涵盖数据库事务隔离、其他用户不受影响、全局配置不变、禁止直接关闭模型绕过迁移、渠道选项隔离，以及运行时替代。数据库 12 项、路由及下线守卫 25 项、模型运行时 80 项通过；已完成双域名同步发布，浏览器验收及边界见下节。

### 2026-09-15 双域同步发布验收

- 发布镜像 `lobehub:lingshu-v2217-group-model-tabs-20260915-r1`，两域同 digest，数据库 / OSS/Redis/QStash 共享配置一致，调度恢复。备份和回退见部署说明。
- Chrome 在 chatdev 验收组页签、Astraflow 标识、组内显示名称即时保存；验收修改已还原，数据库模型配置与发布前基线一致。全览无参数默认全部 / 最近更新。未发起付费模型调用，未实际下线业务模型；迁移隔离由数据库及运行时测试验证。chat.cotti.ai 浏览器待用户登录。

## 历史话题额度与全览默认助理修复（2026-09-15）

- 旧预算冻结先于当前额度校验，导致提高用户或平台额度后，历史话题仍按旧额度拦截。新增统一重算：只有有效额度高于冻结时额度，且已计费用低于新额度，才解除预算冻结并写入审计；话题临时额度、用户额度、平台额度的优先级不变。
- 在额度保存、发送前读取冻结状态、全览列表与详情读取时复核，覆盖历史遗留记录；下一次模型请求仍执行输入及输出预算预留。人工冻结、上下文冻结和关闭限额均不自动解冻。无需数据库 schema 迁移。
- 全览读取其他用户的默认 inbox 助理时，复用原生名称与头像补全，修复空数据库名称被显示为 “未命名助理”；保留自定义助理名称，不改写用户数据。
- 回归用例已验证旧行为失败，修复后 29 项测试、lint 和全仓类型检查通过。部署与浏览器验收结果以部署说明为准。

## 模型开关直接展示下线确认（2026-09-15）

- 修复默认组和用户组模型关闭开关仅滚动到页面底部、状态保持开启而没有明确下一步的问题。点击关闭直接打开下线迁移窗口并预选当前模型，取消不修改配置；确认后继续使用已有渠道隔离、引用迁移及运行时下线保护。
- 复用 TaskModelMigration 和 base-ui 弹窗，没有放开 “下线后旧话题仍可调用” 的路径，没有修改后台模型权限或计费。迁移提交后运行时刷新失败仅提醒，不误报已提交配置回滚。
- 回归确认旧实现的默认组与用户组两个场景失败，修复后均通过；类型检查通过。部署与 Chrome 验收以共享服务部署文档为准。

## 管理员桌面端下载入口（2026-09-15）

- 按用户测试需要，仅在网页版 “执行环境” 精简菜单底部，对服务端确认的管理员恢复原生 “下载桌面端” 链接。普通用户、未登录、权限未就绪或查询失败均隐藏；权限缓存按用户 ID 隔离。
- 复用现有管理员权限接口与官方下载地址，不新增角色、设备网关或自动选设备能力；云端 / 自建沙箱选择不变，桌面客户端原生菜单不变。
- 6 项权限回归、lint 与全仓类型检查通过；部署和 Chrome 验收见共享服务说明。

## 仅金额控制，取消二开上下文冻结（2026-09-15）

- 按最新确认，移除 Terra / Gemini 等模型的二开上下文 Token 冻结阈值及无话题请求长度拦截。官方上下文压缩和模型自身容量限制保持原样，Token 估算仅参与金额预留。
- 保留平台 / 用户 / 单话题有效额度、发送前输入及输出费用预留、超额冻结、调整额度后的预算冻结重算。管理员可提高话题临时额度支持大任务继续，仍受模型原生限制。
- 停应用并备份后，使用 `disable-context-freezes.sql` 原子解除旧 `context` 冻结并记录原值与所属用户审计；预算冻结和人工冻结保留。脚本可重复执行，无 schema 变更。
- 回归验证大上下文不再独立冻结，预算超限仍拦截；12 项守卫测试及 1 项数据库清理 / 审计 / 幂等测试通过，lint、全仓类型检查通过。发布结果以共享服务部署说明为准。

## 首页加载期间模型禁用误报（2026-09-15）

- 原因：二开的可用模型列表在组模型配置或 VIP 权限尚未返回时暂为空，而输入提示仅等待原生模型运行配置就绪，把加载中的空列表判为 “当前模型已禁用”。
- 最小回填：可用模型 Hook 同时提供列表及配置就绪状态，提示等待原生运行配置、模型显示配置和用户权限全部就绪后再判断。所有已有列表调用方接口不变，不增加网络请求、延时或权限放行。真实模型禁用仍即时提示。
- 同一真实列表 Hook 的加载时序回归：旧提示逻辑 2 项失败，新逻辑 27 项通过；lint 和全仓类型检查通过。发布证据见共享服务部署说明。

## 本轮选中连接器的实际工具注入（2026-09-15）

- 飞书资料已连接且权限有效，但客户端发送入口将选中工具说明持久化到消息后，省略了执行器需要的工具选择数据；之前工具引擎接收选中 ID 的兼容不足以覆盖这个真实入口。原生 v2.2.17 的发送入口同样省略该数据。
- 最小回填：`sendMessage → executeClientAgent → internal_createAgentState` 单独传递本轮 `selectedToolIds`，保留旧 `initialContext.selectedTools` 调用方兼容。工具说明仍仅持久化一次，不给 Agent 永久钉选连接器，不跳过现有启用 / 禁用、平台及服务端鉴权。
- 用户给定 Wiki 的飞书 `fetch-doc` 接口真实读取成功，返回 3973 字正文；没有修改文档内容或共享权限。真实失败会话没有飞书工具调用记录，只有沙箱网页读取及登录跳转；没有历史请求完整 tracing，因此不声称已回放历史模型输入。
- 发送入口及工具 ID 生成回归在旧代码上 2 项失败；修复后 149 项相关用例通过。发布与最终类型结果见共享部署文档。

## 2026-09-16：失败附件发送保护与空话题预防

- 生产只读排查发现：失败上传草稿以文件名作临时 ID，旧发送判断仅拦截上传中，error /cancelled 可被继续提交；服务端先建话题，再因消息附件外键不存在而失败，留下无消息、无费用的空话题。
- 新增统一附件就绪选择器，首页和会话页按钮 / 实时发送检查覆盖 pending、uploading、processing、error、cancelled 及未完成解析；拦截时保留文本和附件草稿。用户须重传成功或移除失败附件。
- aiChat.sendMessageInServer 在创建话题、线程和消息前，复用当前用户 / 工作区 FileModel 查找全部附件，缺失或不可访问时返回 BAD\_REQUEST，不写入空话题。不靠 file\_ 前缀判断、不绕过所有权，无 schema 变更。
- 83 项相关测试与全仓类型检查通过；旧代码 7 个新增回归失败。只发布 chatdev.cotticoffee.com/chat.cotti.ai，生产留待统一发布；不删除历史空话题、不修改存储配置。历史上传本身失败的具体网络原因仍需原客户端证据。

### 2026-09-18：未执行话题切换沙箱（双开发域已发布，浏览器验收待补）

- 修正原先 “所有已有话题切换沙箱都必须新开” 的限制：未执行或历史仅为明确授权拒绝时可保留原话题切换；已执行、执行结果不明及运行中继续保护。

- 服务端按话题所有者校验，沙箱调用前后持久化并发标记，附件初始化同样受保护；切换后仍复用现有原始附件同步，不迁移临时工作文件。无 schema 迁移。

- 增加默认关闭的指定话题工具名诊断 `COTTI_TOOL_TRACE_TOPIC_ID`，只记录模型运行时入口的工具名称，不采集正文和参数，不额外调用模型。用于区分工具缺失与模型未调用，不能追溯未记录的历史请求。

- 私有排查与回归证据：`.records/sandbox-switch-20260918/`。不得将旧 `runtimeEnv` 字段当作当前执行目标，实际需检查 `agencyConfig.executionTarget` 与本轮请求。

- 16:20 同步发布 `lobehub:lingshu-v2217-sandbox-switch-20260918-r1`，镜像 digest `sha256:bb866147d382efceacb44ad31c311bdabcc12307d517422b5fba97613f66ad1b`；备份 153 MB 已通过 pg\_restore 列举，双入口健康 / 共享后端及两项 QStash 计划恢复验证通过。32 项首轮及 107 项补充检查通过，全仓类型通过；旧选择逻辑新增回归 1 项失败。没有代用户发消息；历史工具列表不可追溯，已启用指定话题诊断待下次真实请求。

### 2026-09-19：冻结提示持久化兼容补丁

- 运行时代码仅修改 `MessageService.updateMessageError` 一行，把带自定义字段的 Error 转成普通对象，避免 SuperJSON 丢失 type/body，官方严格校验保留。

- 隔离说明及未来上游覆盖后的移除步骤见 [message-error-serialization.md](./message-error-serialization.md)。无需 schema 迁移；金额和冻结状态不变。

- 旧代码真实序列化回归 1 项失败，修复 7 项通过、lint 与全仓类型通过。私有证据 `.records/freeze-error-20260919/`。

- 13:30 双开发域已发布 `lobehub:lingshu-v2217-freeze-error-20260919-r1`，备份及健康 / 调度校验通过。仅补回 1 条已确认的空白错误消息并审计，冻结与额度保留；临时工具诊断关闭。浏览器及付费任务端到端未执行。

### 2026-09-20：旧 auto 执行环境兼容

- 网页端未绑定电脑的原生智能 Agent，旧 auto 经原保存流程恢复沙箱选择。
- 话题提供方不变；委派 Agent 的独立执行环境问题已核对，尚未改动。
- 隔离与移除说明：[sandbox-auto-compatibility.md](./sandbox-auto-compatibility.md)。
- 同批补齐用户端持续冻结提示：预算原因、已用估算金额 / 额度、管理员处理建议；人工及历史上下文原因独立说明。

### 2026-09-20：换题判断先确认指令独立性

- 明确的续接指令前后端共用规则直接留在当前话题；其余最多一次辅助判断。
- 同时满足独立任务与明确换题才弹框，去掉按换题前缀直接弹框的旁路。
- 58 项回归及类型检查通过；细节见 [topic-switch-continuation.md](./topic-switch-continuation.md)。
