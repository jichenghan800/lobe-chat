# OSS 大文件上传分页兼容修复（2026-09-23）

## 故障与边界

生产 Excel 约 70–74 MiB，超过官方 64 MiB 分片上传阈值但未超过约 2 GiB 上传上限。三个分片已存在且大小合计正确。OSS ListParts 在 IsTruncated=false 时仍返回 NextPartNumberMarker；AWS SDK paginator 仅追踪 marker，会出现第一页、空页、第一页循环，无法进入 CompleteMultipartUpload。完整对象尚不存在，后续 HEAD 返回 404。

这不是 Excel 内容切分或知识库切块。原有大表格通过智能模式保留原文件下载链接、避免全文注入上下文的二开保持不变。不得要求用户拆分工作簿作为正式解决办法。

## 最小改动

仅修改 `apps/server/src/modules/S3/index.ts` 的分片列表消费：明确末页立即停止；对重复 marker、截断却无后续 marker / 无分片、页数超出预期分片数拒绝继续。保留已有完整性校验：数量、编号、每片大小，以及服务端读取分片的权限边界。没有改大小上限、Excel 解析或沙箱逻辑，无数据库迁移。

升级时对比官方同一方法，若已正确处理 S3 兼容存储的末页和循环防护，优先移除补丁。开发双入口同步部署；生产基于现有提交 `5fefe5f4bc` 对应的固定运行镜像，只带此修复，避免顺带发布未验收功能。

## 验证

- S3 单测 41 项通过，完整类型检查通过；新增末页与循环回归在旧实现上失败。
- 真实开发 OSS：合成 Excel 75,973,050 字节，3 分片上传并使用修复后的真实 FileS3 方法合并；下载 SHA256 与源文件一致。测试专用对象已清理，没有修改用户文件。
- 下载后的 Excel 已用 openpyxl 逐行验证 20 万行内容一致。部署结果见本次私有记录 `.records/multipart-fix-20260923/`；不将凭据、用户文件或签名 URL 入库。

## 补丁镜像构建与回退

仓库配置 `lockfile: false`、`resolutionMode: highest`，完整重建发现依赖自动刷新。本次未发布这些完整重建镜像，改用 `src/_custom/deploy/multipart-oss/build-patch.cjs`：从修复源码 transpile 提取完整上传方法，精确匹配固定基础镜像中所有对应方法并替换，逐文件 `node --check`；基础镜像其余文件、依赖、前端和数据库迁移均保留。生成 manifest 记录基础镜像 digest 与全部 9 个替换文件；若方法结构变化或目标镜像已存在则拒绝覆盖。下一次正常源码构建自动包含源码修复，无须继续依赖补丁脚本。

候选镜像：开发 `lobehub:lingshu-v2217-multipart-dev-20260923-r2`，生产 `lobehub:lingshu-v2217-multipart-prod-20260923-r2`。候选方法再次完成真实 OSS 三分片合并及下载 SHA256 一致性测试。仅使用合成文件并清理，不恢复或自动重放用户上传。原镜像与数据库备份保留，用于回退。

## 发布完成

- 2026-09-23：开发两个入口同步发布 r2，运行相同镜像，共享后端及两条队列计划校验通过。
- 生产 14:57 完成发布 r2；原环境、依赖容器、调度定义保留。公网认证健康检查 HTTP 200。
- 生产备份 `/opt/lobechat-releases/v2217-multipart-20260923/cutover-backup-20260923-145553/production.dump` 约 385 MiB，归档目录已验证可读；回退脚本保留在同级发布目录。
- 生产 OSS 使用候选镜像中的同一修复方法完成 75,973,050 字节 Excel 三分片合并，下载 SHA256 与原文件一致（合并及下载约 5.2 秒），测试对象已删除。没有重放用户失败的上传，也没有声称用户浏览器复测已经完成；用户刷新后重新上传原文件即可验证其业务文件。
- 上传服务、路由及 S3 相关回归总计 68 项通过；完整类型检查通过。
