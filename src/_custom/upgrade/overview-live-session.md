# 全览模式、原始链接与只读跟踪（2026-09-22）

## 范围

仅开发双入口 chatdev.cotticoffee.com/chat.cotti.ai 同步发布；生产不更新。

原全览按 agent\_operations 是否存在识别智能模式，漏掉浏览器执行的 Agent，误显示为对话。新增用户消息 metadata.cottiInteractionMode 快照，在发送生命周期记录实际工具模式并通过既有 metadata schema 持久化，无数据库迁移。全览取最近记录的模式；后台任务仍优先识别为任务。旧记录仅能根据沙箱 / 电脑工具、委派或服务端执行推断，显示星号及说明；缺少证据显示模式未记录，不按当前 Agent 配置冒充历史模式。

详情标题右侧提供原始话题链接复制，使用当前访问域名与话题所属 Agent；不是公开分享、不会赋予跨用户访问权限。没有普通 Agent 路由的群组 / 工作区话题暂不生成猜测链接。

新增管理员专用 activity 只读接口，沿用 cottiAdminProcedure。只返回轻量版本、最后保存的活动状态 / 时间、模型和已保存的沙箱选择。可见详情每 5 秒检查，版本变化才重新获取完整详情；隐藏页面不轮询。不代发、不修改用户配置、不暴露登录凭据，不监听用户未提交草稿。实际模型流未落库前可能没有新内容；等待状态注明连接状态未知，不将空助手占位当成可证明的活跃任务。

## 回归重点

- 无 agent\_operations 的新智能消息仍标为智能；下一轮切换会话可更新识别；无记录旧话题不误报。
- revision 变化才刷新全文，切话题不误触发旧话题刷新。
- 非管理员拒绝 activity 接口；原始链接保持当前独立域名且不授权越权访问。
- ConversationProvider 使用原有外部 messages 同步，保留用户阅读位置，不强制滚动到底。

升级时优先比较官方模式记录和管理员只读运行观测。功能集中在 CottiTopicOverview 和 cotti/topicOverview；公共改动仅 metadata 可选字段及发送时写入该字段。

验证记录：相关前端 / 服务端 / 发送生命周期回归 132 项通过，管理员路由 3 项通过，完整类型检查通过；镜像构建通过，2026-09-22 两个开发入口已同步部署 `lobehub:lingshu-v2217-overview-live-20260922-r1`，共享数据库 / Redis/OSS 与队列恢复已检查，双入口 HTTP 200。Chrome 登录已过期，页面交互验收待重新登录，不能视为已通过。备份 `/opt/backups/lingshu-overview-live-20260922-r1/before-cutover.dump`；发布证据 `.records/overview-live-20260922/`（私有，不入库）。
