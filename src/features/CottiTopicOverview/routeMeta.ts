import { GalleryVerticalEndIcon } from 'lucide-react';

import { routeMeta } from '@/spa/router/routeMeta';

export const cottiTopicOverviewRouteMeta = routeMeta({
  icon: GalleryVerticalEndIcon,
  titleKey: 'navigation.overview',
});
